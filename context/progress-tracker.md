# Progress Tracker

Update this file after every spec implementation.

## Current Phase

Phase 1 — Backend foundation (Supabase schema, KB ingestion, directory seed).

## Current Goal

All 16 `00-build-plan.md` units are now complete (14 original + Spec 19 hybrid retrieval + Spec 20 capability router — see below). No spec currently in progress — see Open Questions for what's still worth resolving before a live demo.

## Completed

- H0–2: scope locked (`SCOPE.md`), KB topic list finalized, directory seed drafted.
- Project structure and context-file system defined (this file set).
- Spec 01: Project setup & deploy shell (see `context/specs/01-project-setup.md`) — Next.js/TypeScript/Tailwind v4/shadcn scaffold, color tokens wired into `app/globals.css`, empty page. Deployed live: **https://beca-self.vercel.app/**. H2–4 "done" criteria met.
- Spec 02: Supabase schema & migration (see `context/specs/02-database-schema.md`) — `supabase/migrations/0001_init.sql` written and **run against the real Supabase project**, verified live: all 4 tables (`kb_sources`, `kb_chunks`, `directory_entries`, `red_flag_rules`) present with correct columns/types, RLS enabled on all 4 with zero `anon`/`authenticated` policies, pgvector v0.8.2 installed, `idx_kb_chunks_source_id` (btree) + `idx_kb_chunks_embedding` (HNSW cosine) present, `match_kb_chunks` present and smoke-tested. `query_log` deliberately not created (deferred).
- Spec 03: KB ingestion script (see `context/specs/03-kb-ingestion.md`) — `scripts/ingest-kb.ts` written and **run against the real Supabase project**, verified live: all 12 `kb_topics.json` topics ingested (11 fetched from live WHO fact-sheet pages + 1 internally authored), 69 chunks total across all sources, `chunk_index` correctly ordered per source, all embeddings non-null at 1536 dims, spot-checked chunk content matches the live WHO page (no nav/boilerplate captured), `match_kb_chunks` smoke-tested with a real embedded query and returned plausible, correctly-ranked malaria-topic results. First run failed cleanly on OpenAI `429 quota exceeded` (no payment method on the account yet) with zero partial writes; succeeded on re-run after the user added a payment method.
- Spec 04: Directory seed load (see `context/specs/04-directory-seed-load.md`) — `scripts/seed-directory.ts` written and **run against the real Supabase project**, verified live in two parts: (1) `directory_entries` — 11/11 rows inserted from `data/clinic_directory.json` (12 source entries minus the excluded `"disclaimer"` scope-note entry #12), every row's `verified` field correctly mapped from the JSON's mixed boolean/string shape to the DB's `'true'/'false'/'name-only'` text enum, all 9 directory categories present. (2) `red_flag_rules` — `data/red_flag_rules.json` drafted by Claude (67 rows across a 14-category clinical taxonomy: Airway/Breathing/Circulation/Neurological/Cardiac/Stroke/Severe infection/Poisoning/Trauma/Allergic reaction/Pregnancy-postpartum/Children/Mental health/Other acute deterioration), reviewed by the project owner and approved **unedited**, then loaded: 67/67 rows inserted, all `category`/`severity` values valid, every category used has a matching live `directory_entries` row (cross-checked). Both flagged judgment calls (see Architecture Decisions) were accepted as drafted.
- Spec 05: RAG retrieval + chat API (no classifier yet) (see `context/specs/05-rag-chat-api.md`) — **now fully complete.** Originally implemented against Anthropic Claude, then the AI provider was switched to OpenAI (see Architecture Decisions — no Anthropic key available) and `lib/ai/client.ts` rewritten against the OpenAI SDK. All three response paths verified live with the real provider: (1) invalid request → HTTP 400; (2) an out-of-KB question → deterministic `grounded: false` short-circuit, HTTP 200, zero Claude/OpenAI generation call; (3) a real malaria question → `grounded: true`, HTTP 200, with real citations whose `chunk_id`s were cross-checked against the live `kb_chunks` table. A real bug was caught and fixed during this final verification pass — see Architecture Decisions ("citation-metadata trust bug").
- Spec 06: Urgency classifier + escalation branch (see `context/specs/06-urgency-classifier-escalation.md`) — **fully complete, verified live.** Added `lib/ai/classify.ts` (AI urgency classifier), `lib/directory/lookup.ts` (deterministic red-flag keyword check + directory-entry lookup), `app/api/services/route.ts` (`GET /api/services?category=`), and the escalation branch in `app/api/chat/route.ts`, which now runs both checks in parallel on every message and short-circuits to a structured escalation response — never calling `generateAnswer` — when either fires. Live-verified: an exact red-flag phrase escalates via the deterministic check (`emergency` correctly returns both live directory rows, `mental-health` returns its one); a paraphrased stroke description that does **not** substring-match any `red_flag_rules.pattern` still escalated, confirming the AI classifier catches paraphrasing independently; a non-urgent in-KB question still returns the unchanged Spec 05 grounded-answer shape with `escalated: false` added; `GET /api/services` verified for a valid category, an invalid category (400), and a missing category (400); and — the one item that specifically needed live proof, not just code review — the classifier-failure degrade path (Decision 8) was exercised for real by restarting the dev server with `OPENAI_API_KEY` overridden to an invalid value, confirming the route still escalates correctly off the deterministic check alone when the AI classifier 401s and retries out.
- Spec 07: Chat UI shell, mock data (see `context/specs/07-chat-ui-shell.md`) — **implemented and verified.** Added `components/chat/mock-data.ts`, `CitationChip.tsx`, `MessageBubble.tsx`, `EscalationCard.tsx`, `ChatThread.tsx`, and generated `components/ui/input.tsx` via the shadcn CLI; `app/page.tsx` now renders `<ChatThread />` in place of the placeholder. No network calls, no Supabase/AI-provider import anywhere under `components/` — confirmed by repo-wide grep. `npx tsc --noEmit` and `npm run lint` both clean; `npm run dev` started cleanly and `GET /` returned `HTTP 200`. Verified by inspecting the actual rendered HTML (not just visual eyeballing) for every piece of content this spec's decisions require: the grounded malaria answer with its `WHO` citation chip, a second citation with a null `source_url` (added specifically to exercise `CitationChip`'s non-link branch — confirmed it renders as `<span>`, not `<a>`), the verbatim `NO_GROUNDED_INFO_MESSAGE` no-grounded-info bubble, and the escalation card with both mock `matched_entries` (one `verified: 'false'` showing the `(unconfirmed)` contact qualifier, one `verified: 'name-only'` showing "Contact unconfirmed — no number on file" with no contact line) plus the inline disclaimer line. Not machine-verified this pass: the round-robin submit interaction across multiple clicks and keyboard-Tab focus-ring visibility — both rely on unmodified shadcn/base-ui primitives and the same state-append pattern already exercised by the seeded turns, so treated as code-reviewed rather than independently reproduced live; flagged explicitly rather than assumed.
- Spec 08: Wire chat UI to the real API (see `context/specs/08-wire-chat-ui-to-api.md`) — **implemented and verified against the real deployed backend.** Deleted `components/chat/mock-data.ts`; `ChatTurn` moved into `ChatThread.tsx` (its only remaining consumer); `messages` now starts empty (Decision 4 — the zero-message placeholder stays Spec 10's job); submit handler now calls real `POST /api/chat` and appends the live response via the same `setMessages`-append mechanism Spec 07 proved out. Added a single global `status` slot (`idle`/`pending`/`error`, Decision 7) driving a minimal "Thinking…" indicator and a plain error line (Decision 8, 10), both `aria-live="polite"`; input + submit button disabled while pending (Decision 9); `Input` given `maxLength={2000}` (Decision 6); successful responses re-validated client-side with `ChatResponseSchema`/`EscalationResponseSchema` before rendering (Decision 11). `npx tsc --noEmit` and `npm run lint` both clean; `npm run dev` started cleanly. Live-verified with real `curl` calls against the running dev server hitting the real Supabase/OpenAI backend: (1) a real malaria question → `grounded: true`, a real UUID `chunk_id` (not a mock one), and a live WHO citation with its real `source_url`; (2) an out-of-KB question ("best smartphone to buy") → the verbatim `NO_GROUNDED_INFO_MESSAGE` with `escalated: false`; (3) a real red-flag phrase ("baby has a fever and is having convulsions") → `escalated: true` with real live `matched_entries` rows (category resolved to `emergency` by the existing Spec 06 classifier/red-flag logic — that categorization choice is Spec 06's, unchanged here); (4) an empty-message request → `HTTP 400` with the exact safe `"Invalid request body"` string, confirming the client's non-ok error path has a real safe string to surface, never a raw exception. Also confirmed via the rendered homepage HTML: the initial page has no seeded turns (Decision 4) and the message `Input`'s rendered markup carries `maxLength="2000"`. **Not independently verified this pass** — all four require a real browser, not curl/HTML-inspection: the pending indicator actually appearing and clearing in the UI, the input/button's disabled state taking visual effect while a request is in flight, the double-submit guard actually blocking a second overlapping request, and the client `catch` block's `REQUEST_FAILED_MESSAGE` firing on a genuine thrown-fetch failure (as opposed to a server-returned non-ok JSON error, which *was* verified). These are code-reviewed against the written decisions, not reproduced live — flagged explicitly per the same standard Spec 07's entry above set, not assumed to pass.

- Spec 09: Plain-language + Pidgin toggles (see `context/specs/09-plain-language-pidgin-toggles.md`) — **implemented and verified against the real deployed backend.** Before drafting decisions, put a real ambiguity to the project owner: `SCOPE.md`/`project-overview.md` require a distinct "Plain-language toggle" as its own Must-Have feature even though the base `answer` is already required to be plain by the existing system prompt — resolved as **two independent opt-in toggles, both off by default** (see Resolved Questions). `ModelOutputSchema`/`ChatResponseSchema` both gained `simple_version`/`pidgin_version` (always required, non-nullable); `buildSystemPrompt()` extended with generation instructions for both plus an explicit faithfulness rule (no new claim/diagnosis/dosage not already in `answer`); `MAX_COMPLETION_TOKENS` raised 2048→3072; the zero-retrieval short-circuit in `app/api/chat/route.ts` populated from two new fixed constants, `NO_GROUNDED_INFO_MESSAGE_SIMPLE`/`_PIDGIN` (Claude-drafted, not reviewed by a fluent Pidgin speaker — see Open Questions). Added `components/ui/toggle.tsx` (shadcn-generated), `ReadingLevelToggle.tsx`, `LanguageToggle.tsx` (both pure presentational), and gave `MessageBubble.tsx` local `simpleOn`/`pidginOn` state with a documented Pidgin-wins-when-both-on precedence rule — no changes needed to `ChatThread.tsx`, `EscalationCard.tsx`, or `lib/ai/classify.ts`. `npx tsc --noEmit` and `npm run lint` both clean. Live-verified with real `curl` calls against the running dev server hitting the real Supabase/OpenAI backend: (1) a real malaria question returned genuinely distinct `simple_version` (short, plain sentences) and `pidgin_version` (natural Pidgin) text conveying the same content as `answer`, with citations unchanged; (2) an out-of-KB question returned the exact fixed `NO_GROUNDED_INFO_MESSAGE_SIMPLE`/`_PIDGIN` strings; (3) a red-flag phrase's escalation response was confirmed unaffected — no `simple_version`/`pidgin_version` keys present, matching Decision 1's exclusion. **Not independently verified this pass** — needs a real browser, not curl: the toggle buttons actually rendering in the DOM inside a live assistant bubble, their pressed/focus-visible states, the 44×44 touch target taking visual effect, and the "zero new network requests on toggle" claim (Decision 2) actually holding under devtools inspection. These are code-reviewed against the written decisions, not reproduced live — flagged explicitly per the same standard Specs 07–08 set, not assumed to pass.

- Spec 10: Disclaimer, privacy notice, error/empty states (see `context/specs/10-disclaimer-privacy-error-empty-states.md`) — **implemented and verified.** Before drafting decisions, checked whether raw user message text is ever logged server-side, since a privacy-notice claim was about to be written on screen — found a real violation of `architecture.md` hard invariant 4: three `console.error` call sites in `app/api/chat/route.ts` (from Specs 05/06) logged the raw `message` variable directly. **Found and fixed**, not left in place: all three now log `message.length` instead. Added `components/chat/DisclaimerBar.tsx` (one component carrying both the disclaimer and privacy sentences, rendered as `ChatThread.tsx`'s final flex item so its "thin top border" actually separates it from the input above), `EmptyState.tsx` (closes the blank-screen gap Spec 08 Decision 4 deliberately left open, using `app-flow.md`'s own example copy verbatim), and `ErrorState.tsx` (replaces Spec 08's bare inline error text with an icon + message + working "Try again" — deliberately `AlertCircle` not `AlertTriangle`, and neutral tokens only, never `urgent`/`urgent-soft`, which `ui-context.md` reserves for the escalation card alone). Refactored `ChatThread.tsx`: extracted `requestAnswer(message)` out of `handleSubmit` so the retry button reuses the exact same fetch/validate/append logic Spec 08 wrote, and added `lastMessage` state so retry resends without duplicating the user bubble or requiring a retype. `architecture.md`'s `components/chat/` folder listing updated with the three new names (unlike Specs 07/09, these weren't pre-reserved). `npx tsc --noEmit` and `npm run lint` both clean. Live-verified via the rendered homepage HTML: the empty-state copy, the disclaimer sentence, and the privacy sentence all render correctly on a fresh load; the 400 and grounded-answer paths in `app/api/chat/route.ts` still work correctly after the logging edits (confirmed via `curl` against the user's live dev server). **Not independently verified this pass:** the actual server-log output of the three fixed `console.error` calls (would require deliberately breaking the API key on the user's live, shared dev server — the same technique Spec 06 used — chosen not to do without asking first, rather than being unable to) and the "Try again" button's click behavior in a real browser (no browser available this session). Both are code-reviewed correct, not independently reproduced live.

- Spec 11: Test-set script (see `context/specs/11-test-set-script.md`) — **implemented and run for real against the deployed build.** Wrote `tests/test-queries.json` (20 queries: 8 grounded across a spread of real KB topics, 5 clearly out-of-KB refused, 7 escalated spanning multiple `red_flag_rules.json` categories/severities, 2 of the 7 deliberately paraphrased rather than exact-substring matches — same technique Spec 06's own live verification used) and `tests/run-test-set.ts` (zod-validates the query file, resolves target URL via `--target=`/`TEST_TARGET_URL`/a default of the deployed Vercel URL, runs sequentially, classifies each response structurally per category, prints a console table plus two separate headline numbers, writes a gitignored timestamped JSON report). Added `"test-set"` to `package.json`, `/tests/results/` to `.gitignore` (confirmed via `git check-ignore` that a real generated report is actually excluded, not just assumed). `npx tsc --noEmit` and `npm run lint` both clean. **Actually run against `https://beca-self.vercel.app`** (confirmed live and up to date with Specs 09/10 first, via curl) — real result: **overall pass rate 90.0% (18/20)** — meets `project-overview.md`'s ≥90% bar exactly, not comfortably above it — and **escalated-category pass rate 100.0% (7/7)** — meets the stricter bar exactly. Both failures (queries #2 "pregnancy warning signs" and #5 "safe drinking water") were investigated per the spec's own instruction not to quietly patch a failure — not a bug: a standalone diagnostic script (not committed) confirmed retrieval correctly found the right topical sources, and dumped **every** chunk for both sources, confirming neither WHO fact sheet actually contains the practical content the query asks for (the maternal-mortality fact sheet is population statistics/WHO's institutional role, not a patient-facing danger-signs list; the safe-drinking-water fact sheet is global access statistics, not a household water-treatment how-to). The model correctly refused rather than fabricate — hard invariant 1 working as designed. This is a real content-curation gap in two of the 12 `data/kb_topics.json` source URLs chosen at hackathon speed (an already-accepted class of limitation per `SCOPE.md`), not a code defect — logged as a new Open Question below rather than silently patched by swapping the test queries for easier ones, which would have defeated the point of the test.

- Spec 12: Demo polish pass (see `context/specs/12-demo-polish-pass.md`) — **implemented and verified, the final `00-build-plan.md` unit.** Before drafting decisions, put the header question to the project owner first, since it changes the screen's actual composition, not just styling on something already mandated: `ui-context.md` referenced "the header" but `app-flow.md`'s screen list never mentioned one, and the app had zero visible branding anywhere on screen. **Resolved: yes, add one.** Added `components/chat/Header.tsx` (app name only, reused Geist Sans at heavier weight, no new font import) as `ChatThread.tsx`'s first flex item, bookending `DisclaimerBar`'s position as the last one; updated `architecture.md`, `app-flow.md`, and `ui-context.md` so the header isn't left undocumented. Gave `EscalationCard.tsx` an entrance animation (`tw-animate-css` utilities, confirmed present in the installed package version before use, plus `motion-reduce:animate-none`) — deliberately no severity-tiered styling, which would dilute the "unmistakable visual break." Fixed a real, previously-unsurfaced gap in `CitationChip.tsx`: `source_title` was captured in the schema since Spec 05 but never shown anywhere, so every WHO citation displayed identically as "WHO" with no way to disambiguate — added as a `title` tooltip, keeping `ui-context.md`'s compact visual spec unchanged. Added citation deduplication by `(source_title, source_url)` in `MessageBubble.tsx` — a presentational fix only, the API response itself is untouched. Dark mode explicitly named as a deliberate deferral (`ui-context.md`'s own stated "only if polish time allows"), not a silent omission. `npx tsc --noEmit` and `npm run lint` both clean. A repo-wide grep for raw hex values outside `app/globals.css` returned nothing, closing the loop on every prior spec's individual "no raw hex" promise with one aggregate check. Live-verified against the running dev server: the header renders on every load; the escalation response still returns correctly after `EscalationCard.tsx`'s edit; and — genuinely useful, not staged — a real query ("tell me everything about malaria...") returned **5 citations all from the same source**, the exact live case Decision 6 exists for; traced the dedup filter logic by hand against this real 5-item array and confirmed it collapses to exactly 1. **Not independently verified this pass** (no browser available): the animation actually playing and being suppressed under `prefers-reduced-motion: reduce`, and the citation dedup's actual rendered DOM output (the logic was traced correctly against real data, not fabricated, but not visually confirmed in a browser).

- Spec 17: Multi-turn triage clarification — classifier (see `context/specs/17-triage-clarification-classifier.md`) — **implemented and verified live**, the first of the two approved post-Spec-12 build-plan units (14 total now, up from 12). Extended `lib/ai/schema.ts`'s `UrgencyClassificationSchema` from a boolean `urgent` to a 3-way `outcome` (`not_urgent`/`urgent`/`needs_clarification`) carrying up to 2 `clarifying_questions`; added `ClarificationResponseSchema` and `PriorClarificationSchema`; added `needs_clarification: z.literal(false)` to both `ChatResponseSchema` and `EscalationResponseSchema`. `lib/ai/prompts.ts`'s classifier prompt extended with the third outcome plus a final-round addendum used only when a request carries `priorClarification`. `lib/ai/classify.ts`'s `classifyUrgency()` gained an optional `priorClarification` parameter and a 3-way consistency check. `app/api/chat/route.ts`'s request schema gained the optional `priorClarification` field, and the branch order is now: deterministic red flag → AI `urgent` → **round-cap override** (Decision 4: if `priorClarification` was already present and the classifier still returns `needs_clarification`, force-escalate using its best-guess category/severity rather than ask again) → first-round `needs_clarification` → unchanged Spec 05/06 RAG flow. `npx tsc --noEmit` and `npm run lint` both clean. **A real bug was caught during live curl verification, not just code review:** the hand-constructed `EscalationResponseSchema.parse()` call was missing the new `needs_clarification: false` field — `tsc` didn't catch it (zod's `.parse()` takes `unknown`, not the schema's inferred input type), but the first live escalation curl call failed with a genuine `HTTP 500` and a real zod validation error in the dev server log. Fixed and re-verified. Live-verified end to end against the real Supabase/OpenAI backend: a genuinely ambiguous first message → exactly 2 clarifying questions, no escalation, no RAG answer; the same thread replying with an urgent detail via `priorClarification` → correct escalation; the same thread replying with a benign detail → falls through to the unchanged RAG path; a still-ambiguous reply with `priorClarification` present → the round-cap override fired correctly (escalated, never a second question); an exact red-flag phrase on the first message → still escalates immediately, confirming the deterministic net isn't gated behind clarification; malformed request bodies (empty `message`, empty `priorClarification` fields) → `HTTP 400`. **Known, deliberate gap until Spec 18 (UI) ships:** `ChatThread.tsx`'s existing client-side re-validation doesn't yet recognize `ClarificationResponseSchema` — until Spec 18, a real ambiguous message on the deployed build will show the client's generic error state instead of a clarifying question, since the client correctly rejects the unrecognized shape rather than mis-rendering it. Not a regression in already-shipped behavior, but a real temporary UX gap, flagged rather than left implicit.

- Spec 18: Multi-turn triage clarification — UI (see `context/specs/18-triage-clarification-ui.md`) — **implemented**, the second and final approved post-Spec-12 build-plan unit (14/14 now complete). Added `components/chat/ClarificationCard.tsx` — a new component, not a `MessageBubble` variant, same precedent `EscalationCard` already set: bubble-width (not full-width like `EscalationCard`, deliberately less visually loud since `ui-context.md`'s "unmistakable visual break" language is reserved for escalation alone), `border-brand bg-brand/5`, a `HelpCircle` icon + "A couple of quick questions" label, `role="status"` (polite, not `EscalationCard`'s assertive `role="alert"`). Modified `components/chat/ChatThread.tsx`: `ChatTurn` gained a third assistant member (`ClarificationResponse`); new `getPriorClarification(messages)` helper derives the follow-up context from the thread's own state (no new persistence — the server stays fully stateless per Spec 17); new `lastPriorClarification` state stored alongside the existing `lastMessage` (Spec 10) so the "Try again" retry path doesn't lose the clarification context after the optimistic user-turn append already changed the tail of `messages`; `requestAnswer()` gained a second parameter threaded into the POST body; client-side response re-validation extended from a 2-way to a 3-way check (`escalated` → `needs_clarification` → plain `ChatResponse`); the per-turn render branch extended the same way. `context/architecture.md`'s `components/chat/` folder comment and `context/ui-context.md`'s Component specs section both updated with the new component. `npx tsc --noEmit` and `npm run lint` both clean. **Verification scope, flagged explicitly rather than assumed:** this session has no browser available, so the interactive round-trip (typing a reply, seeing `ClarificationCard` render live, confirming the round-cap override reaches the UI as `EscalationCard` rather than a second card, confirming the retry path actually preserves `lastPriorClarification` through a real network failure) was **not independently verified this pass** — the same category of gap Specs 07 and 09 both flagged for their own interactive claims. What *was* verified: the full 3-way discriminated-union typing checks cleanly against the real `lib/ai/schema.ts` types (not stubbed), a real `npm run dev` smoke test returned `HTTP 200` with no compile/render error, and `getPriorClarification`'s "called before the new user turn is appended" contract was traced by hand against `handleSubmit`'s actual append order and confirmed correct at its one call site. The backend contract this UI renders against was already fully live-verified via curl in Spec 17's own pass — not re-proven here, since this spec touched no server code.

- **2026-08-27: Architecture audit — KB coverage gap found and fixed (Phase 0 of a larger, deliberately-deferred plan), outside `00-build-plan.md`'s 14 units.** Reported bug: `"Why is regular physical exercise important for good health?"` returned the fixed no-grounded-information fallback. Investigated the full pipeline (`lib/kb/search.ts`, `lib/ai/classify.ts`/`prompts.ts`/`client.ts`, `app/api/chat/route.ts`, `scripts/ingest-kb.ts`, `supabase/migrations/0001_init.sql`) against a proposed richer RAG architecture (intent detection → topic detection → hybrid retrieval → reranking → evidence-grounded generation) before changing anything. **Root cause, confirmed directly, not assumed:** a pure KB-coverage gap, not a retrieval/threshold/chunking/fallback defect — no physical-activity topic existed anywhere in `data/kb_topics.json`, so `match_kb_chunks` correctly returned zero rows and the deterministic zero-retrieval short-circuit (`app/api/chat/route.ts`, Spec 05 Decision 3) fired exactly as designed. The escalation/safety half of the pipeline (deterministic red-flag check + AI urgency classifier, Specs 06/17/18) was confirmed already correctly separated from KB topic metadata — no change needed or made there.
  - **Broader gaps found but *not* built this pass (explicitly scoped out, not overlooked):** no health-education intent classification (only the urgency classifier exists — a different, already-correct concern), no query expansion, no hybrid/keyword retrieval (`match_kb_chunks` is vector-only; no Postgres FTS column/index exists), no reranking, no A/B/C/D/E-aware fallback logging (today's single fixed refusal message doesn't distinguish "topic not in KB" from "retrieved but judged irrelevant"), and `MIN_SIMILARITY`/`MATCH_COUNT` in `lib/kb/search.ts` are still set from one manual data point (Spec 03), not a labeled precision/recall eval. Presented to the project owner as a phased plan (Phase 0: KB expansion — chosen; Phase 1: hybrid retrieval/query expansion/fallback logging; Phase 2: labeled threshold tuning + intent/topic classification) — only Phase 0 was authorized this session.
  - **What was actually built:** `data/kb_topics.json` expanded from 12 → 21 topics, adding physical activity, hypertension, diabetes, obesity, cardiovascular disease, an NCD overview, tuberculosis, HIV, and asthma — all real WHO fact-sheet URLs, each individually confirmed live via `WebFetch` (title + actual content match) before being committed, not guessed at from a URL-slug pattern. Topic schema extended with `slug`, `description`, `intents`, `keywords`, `related_topics`, `example_questions`, `authority_level`, `status` for **all 21 topics** (retrofitted onto the original 12, not just the new 9, to avoid leaving the file in a half-migrated state) — deliberately kept out of the `kb_sources` DB schema for now (no migration this pass, per the authorized Phase-0-only scope); `scripts/ingest-kb.ts`'s `KbTopicSchema` extended to actually validate these new fields (rather than let zod silently strip them as unknown keys), still only persisting the original fields to `kb_sources`.
  - **Honesty checks applied while writing the new metadata, not just filled in mechanically:** every `example_questions` entry was checked against what each source's real fetched content covers (e.g. topic #8's safe-drinking-water entry explicitly does *not* claim to answer "why is drinking water important" — that's a hydration-benefits question, a distinct still-uncovered topic, flagged in its own `notes` rather than papered over). Each new topic's `red_flag_linked` value and `notes` honestly state whether `red_flag_rules.json` actually has a matching pattern today (hypertension/cardiovascular-disease/asthma: yes, cross-referenced to real rule ids; diabetes/tuberculosis/HIV: no matching rule exists yet, logged as a real gap below, not asserted as covered).
  - **Live-verified, not just code-reviewed:** ran the scoped re-ingestion (`--topics=13,14,15,16,17,18,19,20,21`) against the real production Supabase database — 9/9 topics ingested, 53 new chunks, confirmed directly via a throwaway query script (deleted after use, same pattern Spec 11 used) that `kb_sources` now holds exactly 21 rows and `kb_chunks` exactly 115, with all 12 original rows untouched. Re-ran the exact reported failing query against a live dev server: now returns `grounded: true` with a real, correctly-cited `physical-activity` source and correct `simple_version`/`pidgin_version` text. Spot-checked one pre-existing topic (malaria — still grounded, unaffected), one new topic (hypertension — grounded, correctly cited), the out-of-KB refusal path (still refuses, unaffected), and the emergency-escalation path (still escalates, never reaches RAG, unaffected). Re-ran Spec 11's full 20-query regression suite against the same live dev server: **100.0% overall (20/20), 100.0% escalated (7/7)** — no regression. `npx tsc --noEmit` and `npm run lint` both clean.
  - `architecture.md`'s stack table updated (12 → 21 topics, with a pointer to this entry). No `database-schema.md`, `app-flow.md`, `ui-context.md`, or `code-standards.md` changes were needed or made — this pass touched KB content and the ingestion script's validation only, not the DB schema, UI, or any generation/citation logic.

- Spec 19: Hybrid retrieval, query expansion, fallback diagnostics (see `context/specs/19-hybrid-retrieval-fallback-diagnostics.md`) — **fully complete, verified live**, outside `00-build-plan.md`'s original 14 units but registered as unit 19 (`00-build-plan.md`). Phase 1 from the 2026-08-27 audit, implemented on explicit instruction ("go ahead") after both ⚠️-flagged decisions were confirmed (`match_count` = 8 with 3 keyword-only reserved; keyword-containment expansion over the constrained-LLM-call alternative). New migration `supabase/migrations/0002_hybrid_search.sql` (applied live via a throwaway `pg`-based runner script, not committed): `kb_chunks.content_tsv` generated tsvector column + GIN index, `kb_sources.keywords text[]`, and `match_kb_chunks_hybrid` (vector search unioned with a full-text-search rescue channel; old `match_kb_chunks` left in place, unused). `scripts/ingest-kb.ts`'s `insertSource` now writes `keywords`; re-ran ingestion for all 21 topics (115 chunks, unchanged from pre-Spec-19 — no content drift). `lib/kb/search.ts` rewritten: calls the hybrid function, does bounded keyword-expansion (folds a matched source's full keyword list into the keyword-search text only, never the embedding input), and returns a `matchedVia` field per chunk. `app/api/chat/route.ts`: `searchKb` now wrapped in try/catch (previously an unhandled exception path), plus the audit's A–E `retrieval_outcome` logging at all three no-answer exit points — `message.length` only, never raw text. **Live-verified, not just code-reviewed:** the migration's columns/index/function confirmed directly via `information_schema`/`pg_indexes`/`pg_proc` plus a real RPC smoke test; the exact originally-reported query still returns `grounded: true` with the correct citation; a deliberately awkward paraphrase of it ("Does moving your body a lot help you stay well long term?") also returned `grounded: true` correctly cited; case D (`D_no_match`) confirmed firing in the real server log for an out-of-KB query, logging only `message.length`; Spec 11's full 20-query regression suite re-run with **no regression (100.0%/100.0%, identical to pre-Spec-19)**. **Not independently forced live this pass** (code-reviewed only, flagged explicitly rather than assumed): case C (`C_insufficient_evidence`, fires directly off the already-live-verified `grounded` field) and case B (`B_retrieval_error`, would need a deliberately broken DB/embedding call to force, the technique Spec 06 used for the classifier-failure path — not done here). New devDependencies `pg`/`@types/pg` added and justified in the spec file per code-standards.md hard invariant 5 — used only by the throwaway migration-runner script, never imported by app/lib runtime code.

- **2026-08-28: Fixed-layout + centered-column polish pass, outside `00-build-plan.md`'s units, on explicit project-owner instruction (with reference screenshots of premium chat UIs for direction).** Two reported issues: (1) the app header and input form scrolled away with the message thread instead of staying fixed; (2) on a laptop-width viewport the input spanned the full browser width, reading as oversized. **Root cause of (1), confirmed before fixing:** `app/layout.tsx`'s `<body>` was `min-h-full`, not height-capped — so once the message thread's content pushed the page taller than the viewport, the whole page (not just the thread) scrolled, dragging `Header` and the input form along with it. Fixed by changing `<body>` to `h-dvh overflow-hidden` and `ChatThread.tsx`'s root from `min-h-0 flex-1` (no height ceiling of its own) to `h-full min-h-0` — the message-thread `div` (already `flex-1 overflow-y-auto`) is now the one and only scroll container. **Fix for (2) plus the broader "more premium" ask:** added a shared `mx-auto w-full max-w-3xl` inner column to `Header.tsx`, `DisclaimerBar.tsx`, and the message-thread wrapper in `ChatThread.tsx`, so all four pieces (header, thread, input, disclaimer) align to the same readable-width column on wide viewports instead of stretching edge to edge — each bar/band itself still spans full width for its background/border, only the content inside is capped. The input form itself was redesigned from a plain full-width bordered bar into a single rounded (`rounded-2xl`), bordered, shadowed pill containing both `Input` and `Button` as one visual unit (`focus-within` ring/border for the focused state) — `Input`'s own border/shadow/ring stripped via call-site `className` overrides, not by hand-editing the generated primitive (architecture.md's "do not hand-edit generated files" rule, same precedent Spec 07 Decision 9 set for `Button`'s `min-h-11`/`min-w-11`). No hardcoded hex/pixel values added — only Tailwind width/radius/shadow utilities and existing `ui-context.md` color tokens (`border-line`, `bg-card`, `border-brand`). `npx tsc --noEmit` and `npm run lint` both clean; live-verified via a real `npm run dev` + `curl` against `http://localhost:3000/` — `HTTP 200`, and the rendered HTML confirmed to actually contain `h-dvh`, `max-w-3xl`, and the new pill-input classes (not just present in source, present in the served page). **Not independently verified this pass (no browser available):** the actual pinned/non-scrolling behavior of `Header/DisclaimerBar`/input under real mouse-wheel or touch scrolling, and the input pill's visual appearance/focus-ring on a real laptop-width screen — code-reviewed against the stated fix, not visually confirmed in a browser. `ui-context.md`'s Component specs section doesn't yet have its own "Chat input" bullet (it only lists Header, Message bubble, Citation chip, Escalation/Clarification cards, Toggle, Disclaimer bar) — worth adding if this pill treatment is kept, not done in this pass since it wasn't flagged as missing before the fix.

- **2026-08-28: Layout polish — fixed header/input, capped content width (project-owner direction, outside `00-build-plan.md`).** Two reported issues, both confirmed as real bugs before fixing rather than assumed: (1) the whole page scrolled instead of just the message thread, taking `Header` and the input form with it; (2) on a laptop-width viewport the input spanned the full browser width, reading as oversized. **Root cause of (1):** `app/layout.tsx`'s `<body>` was `min-h-full` (a floor, not a ceiling) inside a `flex flex-col` — once the message thread grew past one viewport, the body itself grew past the viewport and the *page* scrolled, since nothing capped its height. Fixed by changing `body` to `h-dvh overflow-hidden`, giving `ChatThread.tsx`'s root (`h-full`, was `min-h-0 flex-1` with no ceiling of its own) a real fixed-height box to fill; its message-thread `div` (`min-h-0 flex-1 overflow-y-auto`) is now the only element that can scroll — `Header` and the input form sit outside it and stay fixed. **Fix for (2):** wrapped `Header`, the message-thread content, the input form, and `DisclaimerBar` each in an inner `mx-auto w-full max-w-3xl` column — all four now share one consistent content width; the outer bars (header border, input band, disclaimer border) still span full page width so the page doesn't look empty on a wide viewport, only the content inside is capped. The input itself was also restyled from a plain full-width `border-t` bar into a single rounded, bordered, shadowed pill (`Input` + `Button` as one visual unit, border/ring stripped off the generated `Input` primitive at the call site per the existing "don't hand-edit generated files" precedent) — closes the "not premium enough" half of the request, not just the width complaint. `npx tsc --noEmit` and `npm run lint` both clean. **Live-verified via the rendered homepage HTML** (no browser available this session): confirmed `h-dvh`/`overflow-hidden` present on `<body>` and `max-w-3xl` present exactly 4 times (`Header`, thread wrapper, input form, `DisclaimerBar`) in the real output of a running `npm run dev` server, `GET /` returning `HTTP 200`. **Not independently verified this pass** — needs a real browser: the actual scroll behavior (header/input visually staying put while only the thread scrolls), the pill input's `focus-within` ring, and the two Dribbble reference screenshots' influence being visually well-matched rather than just structurally similar. `ui-context.md`'s Component specs section doesn't yet name a dedicated "Chat input" entry — the existing "Input field" spec (48px height, 8px radius) predates this pill treatment; worth reconciling in a future pass rather than assumed to already cover it.

- **Spec 20: Capability router — preventive health, healthcare preparation, service navigation** (see `context/specs/20-capability-router-and-navigation.md`) — **fully complete, verified live (2026-08-28).** Implemented on the project owner's explicit "audit first, then proceed with implementation unless there is a serious architectural blocker" instruction — audit presented first (Parts A–N of that day's task), no blocker found, implementation proceeded in the same session. Closes three of the four named capability gaps (Health Education was confirmed still functional, deliberately not touched, per that day's explicit instruction). Full architecture/decisions/verification detail lives in the spec file; summary: a new capability classifier (`lib/ai/classify-capability.ts`) runs only after the existing safety layer (unmodified) clears a message; `healthcare_preparation` gained a new, deterministic (non-vector) `preparation_checklists` table + lookup, fed into the existing `generateAnswer()` as the sole evidence chunk; `service_navigation` gained a new `directory_entries.services` column + fully deterministic, LLM-free lookup and a new `ServiceResultsCard` UI component; `data/kb_topics.json` grew 21 → 28 topics (oral health, food safety, cholera, hepatitis B, STIs, healthy ageing, newborn care — every URL live-verified via `WebFetch` before being committed); five new `evaluation/*.json` datasets (20+ items each) plus `tests/run-capability-eval.ts` were added, run for real against a live local dev server hitting the real Supabase/OpenAI backend, and their three genuine failures were investigated (not silently patched) — two are real, honest content-specificity gaps (documented, not fixed by loosening the similarity threshold, per that day's task's own explicit instruction not to), one was a wrong eval expectation (an "ambulance" query correctly trips the safety layer before ever reaching the capability router, matching that day's task's own stated priority that safety takes precedence over capability routing) — the eval dataset was corrected, not the code. `tests/run-test-set.ts` (Spec 11) was re-run against the same build afterward: **zero regression — 100.0% overall (20/20), 100.0% escalated (7/7)**, identical to pre-Spec-20. `npx tsc --noEmit` and `npm run lint` both clean throughout. New migration `supabase/migrations/0003_capabilities.sql` applied and verified live against the real Supabase project (via a throwaway runner script, deleted after use — same technique Spec 19 used for `0002`); `npm run seed-directory`, the new `npm run seed-preparation`, and a scoped `npm run ingest-kb -- --topics=22,...,28` were all run for real against production. `architecture.md`, `database-schema.md`, `app-flow.md`, `code-standards.md`, `project-overview.md`, and `00-build-plan.md` were all updated per `ai-workflow-rules.md`'s own doc-sync table.

- **Post-Spec-20 directory addition: Lagos University Teaching Hospital (LUTH), 2 rows — outside `00-build-plan.md`'s units, a data-curation addition rather than a spec, done at the project owner's explicit direction with the facility details supplied by them.** Live-tested `service_navigation` zero-match responses for `imaging`/`dental` (screenshots) prompted the suggestion; before implementing, confirmed with the project owner: (1) this doesn't reopen `SCOPE.md`'s "no other facility data source" lock — hand-curating `data/clinic_directory.json` is the sanctioned mechanism, the same one the original 11 entries used, not a new source; (2) the project owner's source was an informal web search ("I believe... I got this from Google"), not LUTH's own site or a phone call, so both new rows are recorded at the exact same honesty level as the existing `LASUTH`/`General Hospital Lagos` entries — `verified: "name-only"`, contact field says `VERIFY`, and explicitly notes the source is informal, not independently confirmed. Added as **two rows, not one**, on the project owner's confirmation of the drafted plan: `id: 13` "LUTH — Idi-Araba" (`general_outpatient`, `laboratory`, `imaging`, `dental`, `paediatrics`, `emergency`) and `id: 14` "LUTH Harvey Road Annex (Dermatology/Psychiatry Clinic)" (`mental_health` only) — kept separate because the two are physically different locations in different parts of Lagos, and collapsing them into one row risked sending a user to the wrong campus for mental health care. `emergency` was flagged as a judgment call rather than assumed from "they run 24 hours service" (the project owner's own phrasing was tentative) — the project owner explicitly confirmed yes before it was added, and only to the main campus row (`id: 13`), not the annex.
  - **Confirmed, not assumed, that this doesn't touch the safety-critical escalation path:** `directory_entries.services` (Spec 20) and `directory_entries.category` are read by two different, independent code paths — `findByService` (`service_navigation`, calm/informational) vs. `findDirectoryEntry` (the actual emergency-escalation branch, category-keyed). LUTH's `category` is `"general-tertiary"`, same as `LASUTH`/`General Hospital Lagos` — tagging its `services` array with `"emergency"` makes it appear in the calm "where can I get emergency care" navigation answer (now 3 entries) but does **not** add it to the actual escalation card's `matched_entries`, which stays exactly the 2 pre-existing `category: "emergency"` rows (Lagos ambulance, 112). Live-verified directly: a real red-flag phrase ("crushing chest pain that won't go away") still escalates to exactly those same 2 entries, unaffected.
  - `npm run seed-directory` re-run for real against the production Supabase database (14 rows in the JSON → 13 inserted, 1 disclaimer row excluded as always). Live-verified via `curl`: the exact "Where can I get an X-ray or scan?" / "Where can I get dental care?" queries from the reported screenshots now return the new LUTH entry instead of the honest zero-match message; "Where can I get mental health support?" now returns both the Federal Neuro-Psychiatric Hospital and the LUTH annex.

- **Post-Spec-20 directory addition: MedServe-LUTH Cancer Centre (MLCC), 1 row + a new `oncology` navigation service — outside `00-build-plan.md`'s units, done at the project owner's explicit direction with full facility details supplied by them (phone numbers, WhatsApp, hours, official site).** No matching value existed in `NAVIGATION_SERVICES` for oncology/cancer care — added one (`lib/ai/schema.ts`), which the capability classifier's prompt picks up automatically since it interpolates that list directly (`lib/ai/prompts.ts`), plus a human-readable label in `NAVIGATION_SERVICE_LABELS` (`app/api/chat/route.ts`). Confirmed this is the sanctioned "expand the service list later" path Part 11 of `context/specs/20-capability-router-and-navigation.md` anticipated, added on real demand rather than speculatively.
  - **First directory entry independently verified against an authoritative source rather than accepted as project-owner-supplied or "well-known," and the first to actually earn `verified: true`** (every other entry today is `false`/`name-only`/`n/a`). Before committing, fetched `https://nlcc.ng` live (`WebFetch`) and cross-checked the project owner's supplied numbers against it: the organisation name and both phone numbers matched exactly (WhatsApp number matched once formatting differences were normalized); the site did **not** state operating hours or the full street address, so those two specific claims stayed project-owner-supplied/unconfirmed rather than silently upgraded — written into the `area` field's own text as an explicit caveat rather than glossed over, since the schema's single `verified` enum can't express "phone confirmed, hours not" at finer granularity than one flag per row.
  - Real bug caught before it reached the database: the first attempt used `"verified": "true"` (a JSON string) in `data/clinic_directory.json`, which `scripts/seed-directory.ts`'s own `ClinicDirectoryEntrySchema` correctly rejected (it only accepts the JSON boolean `true`/`false` or the string literals `"name-only"`/`"n/a"` — `mapVerified()` is what turns a boolean `true` into the DB's `'true'` text value). Fixed to the unquoted boolean; `npm run seed-directory` then succeeded (15 JSON entries → 14 inserted).
  - Live-verified via `curl`: "Where can I get cancer treatment or screening?" now correctly classifies to the new `oncology` service and returns the MLCC entry with its contact rendered *without* the `(unconfirmed)` qualifier (the first entry in the directory to render that way) — confirmed by reading `ServiceResultsCard.tsx`'s own `verified !== "true"` condition, not assumed. A real escalation phrase re-tested afterward, unaffected. `tests/run-test-set.ts` re-run: still 100.0%/100.0%, no regression. `npx tsc --noEmit` and `npm run lint` both clean.

## In Progress

None — all 16 `00-build-plan.md` units (12 original + the two approved post-Spec-12 clarification units + Spec 19 + Spec 20) are complete. The 2026-08-28 layout/premium-polish pass above is also complete, outside the unit list.

## Next Up

No spec currently queued. Real open items, all logged in Open Questions below rather than left implicit: browser-based verification of Spec 18's live interactive round-trip; the unverified emergency numbers/placeholder clinic entries from `SCOPE.md`; the handful of "should this get a Pidgin/Simple variant too" follow-ups from Specs 09–10; the diabetes/TB/cholera red-flag-rule gaps; Phase 2 from the 2026-08-27 audit (labeled retrieval eval, empirically-tuned thresholds); and, new from Spec 20 (2026-08-28): clinical review of `data/preparation_checklists.json`, the two real content-specificity gaps the capability evaluation surfaced (food safety "at home" framing, general adult preventive care), a `sleep`/`hydration` KB topic once a live-verifiable authoritative source is found, and conversation-memory/follow-up-question resolution (Part 16 of that day's task, explicitly scoped out to protect the Health Education path from regression risk).

## Open Questions

- **From the 2026-08-28 capability router pass (Spec 20) — `data/preparation_checklists.json` needs a real clinical review pass.** Every row's `clinical_review_status` is `'drafted_pending_clinical_review'` — Claude-drafted general appointment-preparation guidance, not reviewed by a clinician. Same standard as the un-reviewed Pidgin refusal copy (Spec 09), lower stakes than `red_flag_rules.json` (which WAS reviewed) since this is logistics/checklist content, not a safety categorization — but should get a project-owner or clinician read-through before being presented as authoritative in a demo.
- **From the 2026-08-28 capability router pass — two real content-specificity gaps surfaced by `evaluation/preventive_health.json`, investigated not silently patched.** "How can I prevent foodborne illness at home?" and "What preventive care should adults consider?" both correctly refuse today: the model judged the retrieved WHO context insufficiently specific to the exact question rather than guess. The first is plausibly fixable with a more targeted food-safety source excerpt; the second may be inherently too broad for a single-source RAG answer to responsibly cover. Worth a future content pass — explicitly not fixed by loosening `MIN_SIMILARITY` or the faithfulness prompt rule, per that day's task's own "don't just lower the threshold" instruction.
- **From the 2026-08-28 capability router pass — no `sleep` or `hydration` KB topic exists yet.** Both are real, honest gaps, not oversights: `hydration` was already flagged during the 2026-08-27 KB expansion (topic #8 explicitly covers water treatment/storage, not fluid-intake importance); a candidate CDC sleep source returned HTTP 403 during this pass's live-URL-verification attempt. Both are reflected as `should_answer: false` entries in `evaluation/health_education.json` with honest notes, not silently dropped. Needs either a different verifiable authoritative source or an internally-authored entry (same pattern as topic #8's water-treatment content) reviewed the same way.
- **From the 2026-08-28 capability router pass — `directory_entries.services` values are inferred from each entry's existing `category`, not independently verified per facility.** Flagged in `data/clinic_directory.json` entry #12's own note, the same cautious treatment `verified` already gives unconfirmed contact numbers — worth a real per-facility verification pass alongside the other unverified `SCOPE.md` items below, not blocking since `service_navigation` already surfaces the same "(unconfirmed)" qualifier as escalation does.
- **From the 2026-08-28 capability router pass — `service_navigation` doesn't filter by location yet.** The capability classifier captures a free-form `location` field (confirmed working live — see `evaluation/service_navigation.json`'s Mushin/Yaba items), but `lib/directory/lookup.ts`'s `findByService` doesn't use it — the seed directory has no lat/long/LGA data to filter against, and adding one would mean inventing facility data `SCOPE.md` locks against. A future real facility dataset (Part 11 of that day's task) would need real, sourced geodata before this becomes usable.
- **From the 2026-08-28 capability router pass — conversation memory / follow-up-question resolution (Part 16 of that day's task) was explicitly scoped out.** The app is still fully stateless per turn (unchanged since Spec 17 Decision 1) — a follow-up like "How often should I do it?" doesn't resolve "it" against the prior turn's topic today, for either the pre-existing Health Education path or the new capabilities. Threading conversation history into `searchKb`/`generateAnswer` would touch the already-verified Health Education generation path, which that day's task explicitly said not to regress — deliberately deferred, not silently dropped.
- **From the 2026-08-27 architecture audit: `red_flag_rules.json` has no pattern for a diabetic emergency (e.g. suspected diabetic coma/severe hypoglycaemia) or a TB-suggestive presentation (e.g. persistent cough + weight loss for weeks).** A severe acute case would still be caught only by the general "Severe infection"/"Other acute deterioration" categories, not a condition-specific one. Flagged in `data/kb_topics.json` topics #15 (diabetes) and #19 (tuberculosis) rather than silently assumed covered. Same review process as Spec 04's original red-flag taxonomy (project-owner sign-off) would apply before adding new rows.
- Which real Lagos neighbourhood/PHC to use for `clinic_directory.json` entries #5, #6, #7, #10 — currently placeholders.
- Whether the Lagos/Nigeria emergency number is verified — not yet confirmed, see `SCOPE.md`.
- **Action needed from the user:** the real Supabase DB password was found pasted as a bare stray line in `.env.example` (a template file meant to hold empty values only) — fixed in the file, but the password itself was live in plaintext outside where a secret should ever sit. Never committed to git, but recommend rotating it anyway (Supabase dashboard → Database → Reset database password), then updating `SUPABASE_DB_URL` in `.env.local` with the new one.
- **Spec 03 risk (non-blocking) — WHO fact-sheet fetch reliability.** 11 of the 12 KB topics fetch live HTML from `who.int` fact-sheet pages (topic #12 is internally authored, no fetch needed — see `data/kb_topics.json`). These are ordinary content pages, not a stable API — markup could change. Implementation approach: fetch with a browser-like `User-Agent`, extract the main article container with `cheerio`, and fail loudly per-topic (log + skip, continue with the rest) rather than silently storing empty/boilerplate content — no cross-run HTML cache or manual-paste fallback built for the MVP. Accepted as a reasonable default given this is a one-off dev-time script, not request-time code.
- **Should the escalation card also get Simple/Pidgin toggles?** Deliberately not built in Spec 09 (see `context/specs/09-plain-language-pidgin-toggles.md` Decision 1) — fixed safety copy (`HIGH_SEVERITY_MESSAGE`/`MEDIUM_SEVERITY_MESSAGE`, the inline disclaimer) shouldn't get a hand-guessed translation without the same kind of owner review `red_flag_rules.json` got in Spec 04. If yes, a small follow-up spec would add reviewed fixed-copy pairs plus a toggle on `EscalationCard.tsx` — not a blocker for the demo's core loop.
- **`NO_GROUNDED_INFO_MESSAGE_SIMPLE`/`NO_GROUNDED_INFO_MESSAGE_PIDGIN` (Spec 09) are Claude-drafted, not reviewed by a fluent Pidgin speaker.** Lower stakes than Spec 04's red-flag taxonomy (non-clinical refusal copy, not a safety categorization), so Spec 09 didn't block on sign-off — but a native-speaker sanity check before the live demo is recommended, same spirit as the existing pre-demo checklist items below.
- **Should the disclaimer bar also get Simple/Pidgin copy?** Deliberately not built in Spec 10 (see `context/specs/10-disclaimer-privacy-error-empty-states.md` Decision 4) — same fixed-safety-copy reasoning used to exclude the escalation card in Spec 09. If yes, would need reviewed fixed-copy pairs the same way `NO_GROUNDED_INFO_MESSAGE_SIMPLE`/`_PIDGIN` did — a small follow-up, not a blocker.
## Resolved Questions

- **Real content-curation gap found by Spec 11's live test run — resolved.** Two `data/kb_topics.json` source URLs didn't actually deliver what their topic title promised. Topic #2 ("Maternal health — risks and warning signs in pregnancy", sourced from WHO's *maternal mortality* fact sheet) contained only mortality statistics/WHO's institutional response — no actual pregnancy danger-signs list, despite `red_flag_rules.json` itself already encoding several concrete maternal danger signs. Topic #8 ("Safe drinking water") was similarly global-statistics-focused, not a household water-treatment how-to. Confirmed at the time by dumping every chunk for both sources (not a retrieval-ranking bug) — both correctly refused rather than fabricated, so never a safety problem, but a real answer-quality gap. **Decided and fixed by the project owner:** Topic #2 retitled to "Pre-eclampsia — warning signs during pregnancy" and re-sourced to WHO's pre-eclampsia fact sheet, which does contain concrete danger signs (severe headache, visual disturbances, upper abdominal pain, swelling, seizures). Topic #8 converted to internally authored (`source_url: null`, `source_name: "internally authored — compiled from WHO & CDC guidance"`), following the exact topic #12 precedent (content added to `scripts/ingest-kb.ts`'s `INTERNALLY_AUTHORED_CONTENT`, not a new storage mechanism) — compiled from CDC's household water treatment guidance and WHO's Household Water Treatment and Safe Storage program page (both cited in the topic's own `notes`), since no single WHO fact sheet covers this practically. Re-ingested with a new, reusable `scripts/ingest-kb.ts` capability (`--topics=<ids>` + `--replace-urls=<old urls>`) that replaces only the targeted sources' `kb_sources`/`kb_chunks` rows — the other 10 topics were left completely untouched, verified directly (all 12 titles/categories/sources queried post-run; chunk count moved 69 → 62, matching removing 12 old chunks and adding 5 new ones exactly). Re-ran Spec 11's test-set script against the deployed build afterward: **100.0% overall (20/20), 100.0% escalated (7/7)** — both previously-failing queries now return correct, specific, cited answers (spot-checked live: the pregnancy query cites the new pre-eclampsia source and lists real danger signs; the water query cites the new internally-authored source with `source_url: null`, rendering correctly through Spec 12's existing null-URL citation handling with no code changes needed).

- **`.gitignore`'s blanket `.env*` pattern excluding `.env.example` — resolved.** Added a `!.env.example` negation line right after the `.env*` rule. Verified both ways before committing: `git check-ignore .env.example` now reports it as trackable, `git check-ignore .env.local` confirms the real secrets file is still ignored. This means `.env.example`'s env-var documentation — updated across Specs 03–05 but never actually reaching git until now — is finally committed.
- **Imminent self-harm risk vs. physical red flags — resolved, no special case.** Was carried forward as an open question through Specs 04–05. Put to the project owner while writing Spec 06: imminent self-harm/suicide-risk red flags (`red_flag_rules.json` ids 59–63) are escalated exactly like any other red flag — same response shape, same fixed severity-keyed copy, same category-based directory lookup (`mental-health` → Federal Neuro-Psychiatric Hospital, Yaba). No crisis-specific copy, no routing override to also surface the emergency number. See `context/specs/06-urgency-classifier-escalation.md`'s "Resolved: mental-health / self-harm escalation handling" section. The one still-open item from that thread — the unverified contact number on that directory row — remains tracked separately under `SCOPE.md`'s pre-demo checklist, not something Spec 06 resolves.
- **What the "Plain-language toggle" actually produces, given `answer` is already required to be plain by the existing system prompt — resolved.** Put to the project owner before Spec 09's decisions were drafted, since `SCOPE.md`/`project-overview.md` require it as a Must-Have feature distinct from the Pidgin toggle, and `lib/ai/schema.ts` already carried a forward-comment planning both a `simple_version` and `pidgin_version` field with no prior spec pinning down what either meant. **Resolved: two independent opt-in toggles, both off by default.** `answer` stays the always-shown baseline; "Simple" ON shows `simple_version` (a further-reduced-reading-level restatement); "Pidgin" ON shows `pidgin_version`. `SCOPE.md`'s "(default-on)" phrasing is read as describing that plain language is the architectural default (already satisfied by `answer` itself), not a literal toggle-starts-flipped state. See `context/specs/09-plain-language-pidgin-toggles.md`'s "Resolved before drafting this spec" section.

## Architecture Decisions

- WHO fact sheets chosen as the sole approved source for the MVP (not NCDC or Lagos State Ministry of Health) — driven by ingestion feasibility at hackathon speed. See `architecture.md`.
- Nigerian Pidgin chosen as the single second-language mode — satisfies both the "multiple languages" and "simplified communication" brief bullets with one feature.
- No end-user auth, by design — matches the brief's privacy emphasis and the do-not-build list.
- **AI provider corrected: OpenAI, not Anthropic Claude** (this entry previously said Anthropic — inaccurate as of the provider swap below; corrected in place rather than left stale, since a future reader would otherwise trust the wrong provider).
- **AI provider switch: Anthropic Claude → OpenAI, for generation/classification (embeddings were already OpenAI since Spec 03).** Reason: no Anthropic API key available (the $20 Anthropic Console credit line in this entry's prior version was never actually usable — Spec 05's live generation path stayed unverified through Spec 05's implementation for exactly this reason). Consolidates the app to one provider instead of two. `lib/ai/client.ts` is the only file that changed — confirmed by design (`architecture.md`'s stack section names this file as the sole swap point) and by a clean `tsc`/`lint` pass touching only that file plus `prompts.ts` (see the boundary-violation note below).
  - **Model: `gpt-5.6-terra`.** OpenAI's own launch positioning for Terra is "high-volume business tasks like customer support, internal tools and document analysis" — a direct match for document-grounded health Q&A — and "competitive performance to GPT-5.5 while being 2x cheaper." Ruled out `gpt-5.6-luna` (cheapest tier, positioned for lower-stakes drafting/summarization — this product's hard invariants around citation faithfulness and never fabricating grounded claims argue for more capability than the bargain tier) and `gpt-5.6-sol` (flagship "hardest problems" tier — coding/security-research grade, unnecessary cost for straightforward RAG QA).
  - **Cost comparison to the original Sonnet 5 plan:** Terra is $2.00/1M input, $12.00/1M output — Sonnet 5 was $2.00/1M input, $10.00/1M output. Close to cost-neutral (Terra's output tokens run ~20% more), not a downgrade in spend to get around not having an Anthropic key.
  - **Latency:** not independently benchmarked at this project's scale (a live single-request curl test succeeded well within the demo's ~5s target — see Spec 05's verify checklist — but that's one data point, not a load test). No latency red flags expected: Terra is explicitly the "balanced" mid-tier, not the slower flagship reasoning tier, and `reasoning_effort: "low"` is set in `lib/ai/client.ts` since this is straightforward RAG QA, not a hard reasoning task.
  - **Structured output mechanism:** `openai.chat.completions.parse()` + `zodResponseFormat()` (confirmed present in the installed `openai@7.5.0`) satisfies code-standards.md's "structured JSON validated against the shared zod schema" requirement exactly as the original Anthropic `messages.parse()` design did — same contract, different SDK call. Confirmed by reading the installed SDK's source directly (not assumed): `chat.completions.parse()` throws a raw `ZodError`/`SyntaxError` on schema-mismatch/invalid-JSON (not wrapped in an `OpenAIError` subclass, unlike Anthropic's `AnthropicError` wrapping) and separately throws `LengthFinishReasonError`/`ContentFilterFinishReasonError` for truncated/filtered output (these do extend `OpenAIError`) — given that mixed taxonomy, `lib/ai/client.ts` catches broadly (any thrown error ⇒ treat as malformed, retry) rather than narrowing to one error class, which would have missed the schema-mismatch case.
  - **Boundary violation found and fixed, not left in place:** `lib/ai/prompts.ts`'s `buildMessages()` previously returned `Anthropic.MessageParam[]` directly — importing the Anthropic SDK's own type into a file that `code-standards.md`'s file-organisation rule and `architecture.md`'s "swap `lib/ai/client.ts` only" both imply should be provider-agnostic. This predates the swap (Spec 05 never exercised a second provider, so it went unnoticed) — it wasn't quietly patched, it's called out here per instruction. Fixed by having `buildMessages()` return a plain `{ role, content }[]` shape instead.
  - **Citation-metadata trust bug found and fixed during live verification, also predates the swap:** the model was asked to fill in each citation's `source_title`/`source_name`/`source_url` itself, but `lib/ai/prompts.ts` never actually gave it the source URL — so it had to guess. Live-tested with a real malaria query: the model correctly cited a real `chunk_id` (that part *was* validated against the retrieved set, per Spec 05's design) but returned `source_url: null` for a source that has a real WHO URL in the live DB. Unlike `chunk_id`, none of that metadata was ever cross-checked — a gap in the same `architecture.md` invariant 5 ("every citation validated against an actual retrieved chunk ID... never trusted unchecked") that Spec 05 otherwise enforces carefully. Fixed by splitting the schema: the model now only reports `cited_chunk_ids` (which chunks it used); `lib/ai/client.ts` reconstructs the full citation objects (`source_title`/`source_name`/`source_url`) from the already-trustworthy `RetrievedChunk` data server-side, never from the model's own output. Re-verified live after the fix — `source_url` now correctly populated.
  - New env var: none — `OPENAI_API_KEY` already existed (Spec 03). `ANTHROPIC_API_KEY` removed from `.env.example` and `.env.local`.
  - `@anthropic-ai/sdk` uninstalled from `package.json` — confirmed via a repo-wide search that no source file imports it any longer before removing it, not just deleted on assumption.
- Tailwind CSS v4 confirmed (already scaffolded in the initial commit, along with Next.js 16.3.2 and shadcn/ui — ahead of Spec 01 being formally written). Tailwind v4 is CSS-first config with no `tailwind.config.ts`; theme values live in an `@theme` block in `app/globals.css`. `ui-context.md` and `code-standards.md` originally said "tailwind.config.ts" (a v3 assumption) — corrected both to point at `app/globals.css`'s `@theme` block instead. User confirmed this direction over creating a `tailwind.config.ts` + `@config` shim.
- `ui-context.md`'s teal interactive-color token renamed from `accent` to `brand` — shadcn/ui already reserves `--accent` internally as a neutral hover-background token used by every generated primitive; reusing the name would cause one value to silently clobber the other in the CSS `@theme` block. User confirmed renaming the brand token rather than overriding shadcn's `--accent`. Use `bg-brand` / `text-brand` / `border-brand` in components going forward, not `accent`.
- HNSW chosen over ivfflat for `kb_chunks.embedding`'s similarity index (`database-schema.md` left this open — "ivfflat (or hnsw)"). This MVP's corpus is 12 WHO fact sheets, likely well under a thousand chunks; ivfflat's `lists` parameter is tuned against row count and needs data present (plus `ANALYZE`) to build a good index, which has no real signal at this scale. HNSW needs no such tuning and gives accurate search from the first row. Uses `vector_cosine_ops` to match the `<=>` operator in `match_kb_chunks`.
- **Embedding provider/model chosen: OpenAI `text-embedding-3-small`** (resolving the "Open decision" in `architecture.md`'s stack table). Outputs 1536 dimensions natively, an exact match to `kb_chunks.embedding vector(1536)` (already applied in Spec 02's migration) with no config or schema change needed. User confirmed over Voyage AI (Anthropic's own recommended embedding partner), which was ruled out only because no Voyage model natively outputs 1536 dims (options are 256/512/1024/2048) and would have required either a new migration to widen/narrow the column or a workaround — Anthropic is still used for all generation/classification; only embeddings come from OpenAI. New env var: `OPENAI_API_KEY` (added to `.env.example`).
- **Chunking strategy chosen: fixed-size token window with overlap** — ~400 tokens per chunk, ~50 token overlap between consecutive chunks within a source, using `cl100k_base` tokenization (the encoding OpenAI's `text-embedding-3-small` uses) via the `gpt-tokenizer` package so "tokens" means the same thing the embedding model sees. Chosen over paragraph/section-based chunking because WHO fact-sheet HTML formatting isn't consistent enough across pages to rely on paragraph boundaries alone; a fixed window is robust to that variance. `chunk_index` increments per chunk within a source, starting at 0.
- **Red-flag rules: both drafted judgment calls accepted as-is** (`data/red_flag_rules.json`, reviewed by the project owner, no edits made). (1) Severe pregnancy danger signs (heavy bleeding, seizures, severe headache + vision changes, postpartum fever, reduced fetal movement) → `category: 'emergency'`, not `maternal-care` — these are "go now" signs and `maternal-care`'s one directory entry is the antenatal clinic, a continuity-of-care destination, not an emergency one. (2) Imminent self-harm risk (stated plan/means, already self-harmed, command hallucinations to self-harm) → `category: 'mental-health'`, not `emergency` — kept under the taxonomy's own category despite the one `mental-health` directory entry (Federal Neuro-Psychiatric Hospital, Yaba) having an unverified contact number. This second call is explicitly **not** fully closed: see Open Questions above — Spec 06 may still need distinct escalation copy/handling for this category, separate from the row's stored `category` value.
- **Spec 06 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
  - `EscalationResponseSchema` and the zero-retrieval/no-grounded-info `ChatResponseSchema` payload are both run through `.parse()` before being returned from `app/api/chat/route.ts`, even though neither is model output — cheap extra insurance that the hand-constructed object actually matches the documented response contract, catching a shape bug at the route layer instead of letting a malformed response reach the client. Not explicitly required by Spec 06's text, but consistent with code-standards.md's structured-output-validation spirit and adds no real cost.
  - The escalation branch's category/severity resolution (Decision 6) includes an explicit, documented "should be unreachable" guard: if neither the deterministic match nor the AI classifier actually yields a resolvable category+severity inside the branch that only runs when at least one of them fired, the route logs and returns the generic 500 rather than assuming the combination logic can never fail. This mirrors Decision 9's own reasoning (`matched_entries` handling a zero-row case that "shouldn't happen" but is still coded for) applied to one more spot.
  - Verified the classifier-failure degrade path (Decision 8) by overriding `OPENAI_API_KEY` via shell env on a restarted dev server rather than `.env.local` — Next.js's env precedence puts `process.env` above `.env.local`, so this exercised a genuine 401/retry/degrade cycle without touching the real secrets file.
- **Spec 07 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
  - Mock fixtures live in `components/chat/mock-data.ts`, not under `lib/` — `lib/ai/`, `lib/kb/`, `lib/directory/` are reserved for real provider/DB code per `code-standards.md`'s file-organisation rule, and a fixtures file that Spec 08 deletes wholesale doesn't belong there. Keeps this spec's footprint entirely inside `components/chat/` plus one new generated file, `components/ui/input.tsx`.
  - The mock data reuses real fixed-copy constants from `lib/ai/schema.ts` (`NO_GROUNDED_INFO_MESSAGE`, `HIGH_SEVERITY_MESSAGE`) and is typed against the real `ChatResponse`/`EscalationResponse`/`Citation`/`DirectoryEntry` types from the same file — a type/constant import into `components/`, not a provider-SDK import, so it doesn't trip `code-standards.md` hard invariant 3.
  - `EscalationCard` is a hard-separate component from `MessageBubble`, never a bubble variant, matching `app-flow.md` state 3's "never rendered as a plain chat bubble." `ChatThread.tsx` branches per-turn on the `escalated` discriminant.
  - `MessageBubble`'s grounded vs. no-grounded-information rendering is derived from `response.grounded`, not a separate prop — keeps the component unable to disagree with the data it's given.
  - Directory entries with `verified !== 'true'` render a visible "(unconfirmed)" qualifier in the escalation card itself — surfaces `SCOPE.md`'s known-unverified-contact-info caveat where a user would actually act on it. `verified: 'name-only'` additionally suppresses the (null) contact line and states plainly that no number is on file.
  - The interactive submit path is a throwaway 3-item round-robin mock-response queue, not reactive to typed content — proves the `setMessages`-append mechanism Spec 08 will reuse verbatim, without building any real classification logic in a UI-only spec (`ai-workflow-rules.md`'s split rule).
  - New shadcn-generated primitive: `components/ui/input.tsx`, via `npx shadcn add input` — not a new npm dependency (`shadcn` was already a devDependency since Spec 01).
  - No `architecture.md`, `database-schema.md`, `app-flow.md`, `ui-context.md`, or `code-standards.md` changes were needed — this spec only fulfills folder-structure entries and UI specs those files already documented; confirmed by re-checking each before writing the spec.
- **Spec 08 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
  - The fetch call lives inline in `ChatThread.tsx`'s submit handler, not a new hook — one call site doesn't earn a `hooks/useChatSession.ts` yet, and calling a same-origin `/api/chat` route (not a provider SDK) doesn't trip `code-standards.md`'s "components never call Supabase or the AI provider directly" rule.
  - The seeded mock turns and round-robin queue were removed, not adapted — the thread now starts genuinely empty. The zero-message empty-state placeholder (`app-flow.md` state 1) stays unbuilt on purpose until Spec 10; this is a deliberate, documented gap between Specs 08 and 10, not an oversight.
  - Request status (`idle`/`pending`/`error`) is one global slot, not a per-turn field — this app only ever has one request in flight, so a per-turn status would have added complexity `ChatTurn`'s real-response-mirroring shape didn't need.
  - The pending indicator is a single `aria-live="polite"` text line ("Thinking…"), deliberately not a skeleton or typing-dots animation — that visual polish is explicitly `00-build-plan.md` unit 12's job, not this wiring unit's.
  - Error copy is sourced from the server's own safe `error` string when the response is non-ok JSON (verified live: an empty-message request returns exactly `"Invalid request body"`), falling back to a client-only `REQUEST_FAILED_MESSAGE` constant only when the response body isn't valid JSON at all (network-level failure) — never a raw exception or stack trace reaches the UI either way.
  - Successful responses are re-validated client-side with `ChatResponseSchema`/`EscalationResponseSchema` before rendering, not trusted on the `escalated` discriminant alone — cheap insurance against a transport-layer shape bug, mirroring Spec 06's own route-level `.parse()` pattern on hand-constructed objects.
  - No `AbortController`, timeout, or unmount-cancellation logic — accepted simplification for a single-screen app with no navigation away from the chat and no evidence yet of latency requiring it.
- **Spec 09 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
  - Toggle scope is grounded/no-grounded-info answers only (`ChatResponseSchema`) — the escalation card was deliberately excluded, not silently dropped nor guessed at; see the Open Question above.
  - `simple_version`/`pidgin_version` are generated eagerly in the same completion as `answer`, whenever the model runs — including when the model itself sets `grounded: false` — so toggling never triggers a new request, per `app-flow.md` Journey 3.
  - The deterministic zero-retrieval short-circuit (which never calls the model) gets its own fixed, hand-authored `NO_GROUNDED_INFO_MESSAGE_SIMPLE`/`_PIDGIN` constants instead — flagged as Claude-drafted and not yet reviewed by a fluent Pidgin speaker (Open Question above), the same way `red_flag_rules.json` was flagged in Spec 04, though lower stakes since this is refusal copy, not a safety categorization.
  - Faithfulness (no new claim, drug name, dosage, or diagnosis not already in `answer`) is enforced at the prompt level only — `architecture.md` hard invariants 1–2 apply to the two new fields exactly as they already apply to `answer`, with no new runtime validation code, consistent with the existing enforcement style (there's no equivalent automated check on `answer` itself beyond the citation/grounding-consistency check).
  - Toggle state lives per-`MessageBubble` instance (two local booleans), not globally on `ChatThread` — matches "toggles... on an existing answer" in `app-flow.md`; no prop-shape change was needed anywhere else since `ChatResponse` already carries all three text variants once returned.
  - When both toggles are on, Pidgin wins — a documented tie-break, not a fourth "simple Pidgin" variant; each toggle still reflects its own pressed state independently.
  - `MAX_COMPLETION_TOKENS` raised 2048→3072 to cover three generated text fields instead of one.
  - New shadcn-generated primitive: `components/ui/toggle.tsx`, via `npx shadcn add toggle` — confirmed its underlying `@base-ui/react/toggle` primitive uses controlled `pressed`/`onPressedChange` props (read directly from the installed package's `.d.ts`, not assumed) before wiring `ReadingLevelToggle.tsx`/`LanguageToggle.tsx` against it. Not a new npm dependency — same shadcn-CLI pattern as `Input` (Spec 07) and `Toggle` itself being CLI-generated code, not a package add.
  - No `'use client'` directive added to `MessageBubble.tsx` despite it gaining local `useState` — it's only ever rendered as a descendant of `ChatThread.tsx`, which already declares the directive (Spec 07 Decision 3); the client boundary starts there.
- **Spec 10 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
  - Found and fixed a real hard-invariant-4 violation predating this spec: `app/api/chat/route.ts` had three `console.error` calls (Specs 05/06) logging the raw user `message` directly — server logs are typically retained, which is exactly the persistence the invariant forbids. Fixed to log `message.length` instead, at all three sites. Discovered specifically because drafting the privacy-notice copy this spec adds required first confirming the claim it makes ("nothing you type is saved") was actually true.
  - `DisclaimerBar` carries both the disclaimer and privacy sentences in one component, not two — `project-overview.md` lists them as a single Must-Have bullet. Positioned as `ChatThread.tsx`'s final flex item (below the input, not above it) so `ui-context.md`'s "thin top border" spec actually reads as a separator from something, not floating at the top of the page with nothing above it to separate from.
  - `EmptyState` uses `app-flow.md`'s own example copy verbatim rather than inventing new text, and deliberately has no clickable "tap to fill" affordance on the sample question — kept as descriptive text only, matching what `app-flow.md` actually calls for.
  - `ErrorState` deliberately uses `AlertCircle`, not `AlertTriangle` (already `EscalationCard`'s icon), and only neutral tokens (`ink`/`ink-soft`/`line`/`card`) — never `urgent`/`urgent-soft`, which `ui-context.md` reserves for the escalation card "never used for anything else." Its "Try again" control uses the generated `Button` primitive with `variant="link"`, explicitly overridden to `text-brand` — confirmed by reading `app/globals.css` that shadcn's own `--primary` token is a separate grayscale value, not an alias for `--brand`, so the link variant's default color wouldn't have matched `ui-context.md`'s "brand = interactive elements, links" rule without the override.
  - `ChatThread.tsx`'s `handleSubmit` was refactored to extract `requestAnswer(message)`, shared by both the form submit and the new retry button, plus a `lastMessage` state — the retry button resends the exact failed message without appending a second user bubble (the original is already visible from the optimistic append) or requiring a retype.
  - `architecture.md`'s `components/chat/` folder-structure comment updated with `EmptyState, ErrorState, DisclaimerBar` — unlike Spec 07's and Spec 09's components, these three names were never pre-reserved there, so the doc needed a real update, not just fulfillment of an existing name.
  - The actual server-log output of the three fixed `console.error` calls was not independently triggered this pass — doing so would require deliberately breaking the API key on the user's live, shared dev server (the technique Spec 06 used to verify its own classifier-failure path), which wasn't done without asking first. The fix itself was verified by direct code review of the edited lines instead.
- **Spec 11 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
  - `tests/test-queries.json` and `tests/run-test-set.ts` live under the previously-empty, already-reserved `tests/` folder, not `scripts/` — `scripts/`'s own folder comment is specific to one-off scripts that write *into* Supabase; this one only reads from the deployed HTTP API, a genuinely different purpose.
  - Pass criteria are structural, not content-graded: `grounded`/`refused` check the response shape (including Spec 09's `simple_version`/`pidgin_version` presence); `escalated` checks only `escalated === true` — `category`/`severity` are recorded as an informational match/mismatch column that never flips pass/fail, since `project-overview.md`'s stated goal is about the escalation firing, not about which category the classifier happened to pick.
  - Two separate headline numbers are printed (overall, and the `escalated` category alone) rather than one blended rate, matching `project-overview.md`'s two differently-worded goals (≥90% overall, 100% for red-flag queries).
  - Ran against the deployed build (`https://beca-self.vercel.app`), not localhost, per the build plan's own wording — confirmed live and current with Specs 09/10 first, via curl, before running the full set.
  - **Real result: 90.0% overall (18/20), 100.0% escalated (7/7).** Both bars technically met. The two failures were investigated with a standalone, uncommitted diagnostic script rather than dismissed or worked around by changing the test queries — see the new Open Question above for the actual finding (a real content gap in two `kb_topics.json` source URLs, not a code defect). This is the kind of finding a test-set script exists to surface, not something to quietly patch away by loosening pass criteria or swapping in easier queries.
  - `tests/results/` confirmed gitignored via `git check-ignore` against a real generated report file, not just assumed from the `.gitignore` entry's presence.
- **Spec 12 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
  - The header question was put to the project owner before drafting any decisions, not decided unilaterally — a new visible top-level UI element changes the screen's actual composition (`app-flow.md`'s explicit contents list), unlike the smaller in-scope implementation choices made unprompted elsewhere in this build (icon picks, token usage). Resolved: add it, reusing the existing font rather than importing a new one.
  - `EscalationCard.tsx`'s entrance animation uses `tw-animate-css` utility class names confirmed present in the installed package version (via its README) before being used — not assumed correct from general `tailwindcss-animate` convention alone.
  - `source_title` had been captured in `Citation`'s schema since Spec 05 but never rendered or exposed anywhere in the UI until this spec — every citation for a WHO source was indistinguishable from every other. Fixed via a `title` attribute rather than a visible layout change, keeping `ui-context.md`'s literal compact chip spec intact.
  - Citation deduplication is presentational (`MessageBubble.tsx`), not a schema or `lib/ai/client.ts` change — `architecture.md` hard invariant 5 governs citation validity, not how many chips a compact UI renders for a validated set; a real live query surfaced 5 identical citations from one source, confirming this wasn't a hypothetical case.
  - Dark mode was explicitly logged as deferred rather than silently skipped, since `ui-context.md` itself frames it as optional and this is otherwise the natural spec to have raised it in.
- **Post-Spec-11 content-gap fix (topics #2, #8) — outside `00-build-plan.md`'s 12 units, a content correction rather than a spec, done at the project owner's explicit direction with exact replacement values and body content supplied.** `scripts/ingest-kb.ts` gained two new CLI flags: `--topics=<ids>` (scope which topics from `data/kb_topics.json` get (re-)ingested) and `--replace-urls=<old urls>` (delete exactly those existing `kb_sources` rows, by their current `source_url`, before inserting). Added because `kb_sources` has no column linking back to `kb_topics.json`'s numeric `id`, and topic #8's category (`hygiene`) is shared with topic #7 — category-based matching would have risked touching the wrong row. Explicit old-URL-based deletion was chosen over a fragile title-matching heuristic or a new migration (`kb_sources` schema change) just for a one-time correction — both flags are genuinely reusable for any future single/multi-topic swap, not a one-off hack. Topic #8's new body content was added to `INTERNALLY_AUTHORED_CONTENT` in `scripts/ingest-kb.ts` — the exact same mechanism topic #12 already used, per explicit instruction not to invent a new storage convention for one entry.
- **Post-Spec-12 font-system change — outside `00-build-plan.md`'s 12 units, reopens Spec 12's "no second font import" decision, at the project owner's explicit direction after reviewing a side-by-side mockup comparison (an Artifact, not committed to the repo).** Replaced Geist Sans/Geist Mono with Public Sans (body/UI), Big Shoulders (display/heading, header only), and IBM Plex Mono (mono, currently unused beyond wiring) — all loaded via `next/font/google` in `app/layout.tsx`. `ui-context.md`'s Typography section and Header component-spec bullet updated accordingly; `12-demo-polish-pass.md`'s own historical text ("reuses Geist Sans... no new font import") deliberately left unedited, same precedent as the app-rename entry above — it was factually true when written.
  - **Real bug found and fixed, not carried forward:** the previous `--font-sans: var(--font-sans)` line in `app/globals.css`'s `@theme inline` block never actually resolved to Geist Sans — `app/layout.tsx` set Geist's CSS variable as `--font-geist-sans`, a name `--font-sans` never referenced anywhere. The `font-sans` utility (and `html`'s base `@apply font-sans`) had been silently falling back to the browser/Tailwind default stack since Spec 01; Geist Sans was imported but never actually applied to any text. Fixed by naming the new fonts' `next/font` `variable` options to exactly match the `@theme inline` token names they feed (`--font-sans`, `--font-mono`, and a new `--font-display` feeding the pre-existing but previously-unused `--font-heading` alias) — the documented next/font + Tailwind v4 CSS-first pattern, where `@theme inline` reads the variable live from the cascade rather than needing a literal value at parse time.
  - **`Big_Shoulders_Display` doesn't exist as a separate export in the installed `next/font/google`** — this snapshot exposes Google's several Big Shoulders widths/optical sizes as one `Big_Shoulders` family with an `opsz` axis instead of a separate "Display" family function. Used `Big_Shoulders({ weight: "variable", axes: ["opsz"] })` — `axes` errors at build time unless `weight` is `"variable"` (a fixed weight array isn't allowed alongside it), so the actual header weight comes from Tailwind's `font-bold` utility on `Header.tsx`, within the font's variable range, not from the font loader's `weight` option.
  - Verified live, not just code-reviewed: `npx tsc --noEmit` and `npm run lint` both clean; started a real dev server and fetched both the rendered homepage HTML (confirmed `<html>` carries all three generated font-variable classes, and `Header.tsx`'s wordmark renders with `font-heading font-bold uppercase tracking-wide`) and the actual compiled CSS chunk, confirming `--font-sans`, `--font-mono`, and `--font-display` each resolve to real `@font-face` family names (`"Public Sans"`, `"IBM Plex Mono"`, `"Big Shoulders"`) and that `font-heading` correctly chains to `--font-display`. One harmless dev-only warning seen in the server log ("Failed to find font override values for font `Big Shoulders` — skipping generating a fallback font") — cosmetic (next/font just skips an automatic fallback-metrics adjustment for this face), not an error; page still returns 200.
  - New npm dependency: none — same `next/font/google` mechanism already used for Geist, just different font names requested from Google's catalog.
- Direct Postgres connections to Supabase (`db.<ref>.supabase.co`) are IPv6-only (no `A` record) and unreachable from this build sandbox's network. Migrations must run through the **Session mode connection pooler** (`aws-*.pooler.supabase.com`, port 5432) instead — this is Supabase's own documented fix for IPv4-only environments, not a workaround. In practice the migration also succeeded through the Transaction-mode pooler (port 6543) for this single-shot multi-statement DDL run, but Session mode is still the one to reach for by default — Transaction mode's per-statement connection multiplexing is not guaranteed to hold up for more complex future migrations.
- **Approved post-Spec-12 enhancement: multi-turn triage clarification, deepening Spec 06 rather than adding a new capability area.** When the urgency classifier can't confidently determine urgency from a single message alone, it may ask up to two targeted clarifying questions in the same thread before deciding, instead of forcing a premature normal-answer/escalation call on insufficient information. Added to `project-overview.md`'s new "Approved Post-MVP Enhancements" section (kept separate from the original locked Must-Have list so build history stays honest about what was in-scope at MVP time vs. approved afterward), `app-flow.md` (new Clarification state + Journey 4), and `00-build-plan.md` (units 17–18, total 12 → 14). Split into two units per `ai-workflow-rules.md`'s own splitting rule (never combine a UI change and an API-route change): unit 17 (classifier/schema/route, curl-verified, no UI) depends on 06; unit 18 (UI rendering) depends on 17.
  - **Scoped to exactly one round, two questions max — not open-ended follow-up.** Continued uncertainty after the single clarifying reply resolves to escalation, never a second round of questions. This is a deliberate, narrow bound for two reasons: (1) `project-overview.md`'s out-of-scope line explicitly forbids "any autonomous multi-step agent behaviour" — an unbounded clarification loop would drift toward exactly that, while a hard-capped single round stays a bounded, user-initiated exchange in the same stateless-thread pattern Journey 2 already uses (a follow-up message re-running classification), not an agent making its own decisions about when to stop asking; (2) safety bias — this classifier's job is triage for a health app where the cost of under-escalating a genuinely urgent message is much higher than the cost of one unnecessary escalation, so when uncertainty survives a single clarifying exchange, the correct default is to escalate rather than keep probing and risk delaying care.
  - **Spec 17 implemented and verified live** — see its own Completed entry above and `context/specs/17-triage-clarification-classifier.md`. Both ⚠️-flagged decisions in the drafted spec (the `priorClarification` request-contract addition reopening Spec 06 Decision 1; the route-level round-cap override) were approved as drafted, without edits, when implementation was authorized.
  - **Spec 17 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
    - `UrgencyClassificationSchema.outcome` (`not_urgent`/`urgent`/`needs_clarification`) replaced the old boolean `urgent` field outright, not added alongside it — no other file referenced the old field (confirmed via repo-wide grep before removing it), so this was a clean rename/restructure, not a breaking change against any live caller.
    - `category`/`severity` are a required best-guess on `needs_clarification` outcomes too, not left null — the one piece of the design that makes the route-level round-cap override (Decision 4) possible without a second model call: if the classifier still returns `needs_clarification` on a reply that already carried `priorClarification`, the route force-escalates using values the classifier already provided, never hits a null-category gap.
    - The one-round cap is enforced twice, not once — a prompt-level instruction (`buildFinalRoundAddendum()` in `lib/ai/prompts.ts`, only included when `priorClarification` is present) plus a structural route-level override (`forcedUrgentOverride` in `app/api/chat/route.ts`) that fires regardless of whether the model actually obeyed the instruction. This mirrors Spec 06 Decision 9's "should be unreachable, still coded for" precedent, applied to the actual safety backstop for this feature's scope boundary rather than a formatting edge case.
    - **Real bug found and fixed via live verification, not code review alone:** `EscalationResponseSchema.parse({...})` in `app/api/chat/route.ts` was missing the new `needs_clarification: false` field in its object literal. `npx tsc --noEmit` passed regardless, because zod's `.parse()` accepts `unknown` input rather than being typed against the schema's own inferred shape — a missing-required-field bug at a `.parse()` call site is invisible to the TypeScript compiler. The first live escalation curl test caught it immediately: `HTTP 500` with a genuine zod `"invalid_value ... expected false"` error in the dev server log. Fixed by adding the field; re-verified live afterward, passed.
    - A genuine, temporary UX gap was surfaced and documented rather than silently accepted or silently fixed out-of-scope: `ChatThread.tsx`'s existing client-side response re-validation (Spec 08 Decision 11) doesn't recognize `ClarificationResponseSchema` yet, so between this spec and Spec 18 a real ambiguous message on the live deployed build will show the generic error state, not a clarifying question. Not fixed here — `ai-workflow-rules.md`'s splitting rule explicitly forbids combining a UI change into an API-route spec — but flagged explicitly, the same standard this tracker has applied to every other known-gap entry above (e.g. Spec 08's four browser-only unverified items, Spec 09's Pidgin-copy caveat). **Closed by Spec 18**, see below.
  - **Spec 18 implemented** — see its own Completed entry above and `context/specs/18-triage-clarification-ui.md`. Decision 2's ⚠️-flagged visual design (no pre-existing `ui-context.md` entry to build against) was approved as drafted, without edits, when implementation was authorized — closing the live UX gap Spec 17 flagged.
  - **Spec 18 implementation decisions, all matching the written spec — logged here per the sync table rather than left only in the spec file:**
    - `ClarificationCard` is a new component, not a `MessageBubble` variant or a prop on `EscalationCard` — matches `app-flow.md` state 6's explicit "distinct from both" requirement and the exact precedent `EscalationCard` itself already set in Spec 07 (a state with its own meaning gets its own component).
    - Deliberately less visually loud than `EscalationCard`: bubble-width, not full-width; `brand` teal, not `urgent` red; `role="status"`, not `role="alert"`. This preserves `ui-context.md`'s "unmistakable visual break... single highest-value asset" language as something escalation alone owns — giving a routine triage question the same visual weight as a safety alert would have diluted the contrast that language is protecting, the same concern Spec 12 Decision 3 raised when deciding against severity-tiered escalation styling.
    - The follow-up context (`priorClarification`) is derived from `ChatThread.tsx`'s own existing `messages` state via a small pure helper, not new component state or any server-side session — kept the server's Spec 17 Decision 1 statelessness intact; the client is only ever supplying back what it already rendered.
    - A second piece of state, `lastPriorClarification`, was added specifically because `getPriorClarification()` can only be correctly computed from `messages` *before* the new user turn is optimistically appended — the existing `lastMessage` state (Spec 10) already solved the identical "the retry button runs later, after that append" timing problem for the message text itself, so the same pattern was reused rather than inventing a different one for the clarification context.
    - No client-side clarification-round counter was added, even though it would have been easy to bolt on — the one-round cap is already fully enforced server-side (Spec 17 Decision 4), and a second, client-side copy of that same guarantee would be redundant defensive logic with no real failure mode it protects against, the same "don't duplicate a server-side invariant in the client" restraint Spec 08 already showed elsewhere.
    - No new input mechanism, no contextual "answering a clarification" placeholder/copy variant — the existing input/button/`handleSubmit` flow answers a clarification exactly the way it asks any other question, matching `00-build-plan.md` unit 18's own wording ("no new input mechanism") read as covering copy surfaces too, not just the literal form element.
    - **Verification gap, flagged rather than assumed:** this session has no browser, so the actual interactive round-trip — `ClarificationCard` rendering live, the request body actually carrying `priorClarification`, the round-cap override reaching the UI as `EscalationCard` and not a second clarification card, the retry path surviving a real network failure with `lastPriorClarification` intact — was not independently reproduced. What was verified: full `tsc`/`lint` cleanliness against the real 3-way discriminated-union types (not stubs), a real `npm run dev` smoke test (`GET /` → `HTTP 200`, no compile/render error), and a hand-trace of `getPriorClarification`'s timing contract against `handleSubmit`'s actual append order. Recommended before the live demo: a real browser pass through the full clarification flow, the one item left in Next Up.

## Session Notes

Read `SCOPE.md` and the "Beca" strategy document for full product/judging context before starting Spec 01 — this tracker assumes that context is already known and does not repeat it.

- 2026-08-25: `CLAUDE.md` was missing a formal pointer to `SCOPE.md` and `context/specs/00-build-plan.md` — both were referenced repeatedly elsewhere but not listed as required reading. Added a "Before starting any spec" section to `CLAUDE.md` covering both files.
- 2026-08-26: Implemented and fully live-verified Spec 06 (urgency classifier + escalation branch) per its written spec, with no deviations from the plan. `npx tsc --noEmit` and `npm run lint` both clean throughout.
- 2026-08-26: Wrote and implemented Spec 07 (chat UI shell, mock data) in the same session. During implementation, extended the original mock-data draft to add a second citation with a null `source_url` specifically so `CitationChip`'s non-link rendering branch would be genuinely exercised in the verify pass rather than only code-reviewed — re-fetched the rendered HTML afterward and confirmed the fix. No other deviations from the written spec.
- 2026-08-26: Wrote and implemented Spec 08 (wire chat UI to the real API) in the same session. No deviations from the written spec. Live-verified the three response paths (grounded, no-grounded-info, escalation) plus the 400 safe-error path directly against the running dev server and the real Supabase/OpenAI backend via `curl`, and confirmed the empty initial thread and `maxLength="2000"` via the rendered homepage HTML. The four purely browser-interaction items (pending indicator visibility, disabled-state visuals, double-submit blocking, and the thrown-fetch-error catch path) were not independently reproduced this pass — no browser available in this session — and are flagged as code-reviewed only, not assumed passing.
- 2026-08-26: Wrote and implemented Spec 09 (plain-language + Pidgin toggles) in the same session, after resolving the "Plain-language toggle" ambiguity with the project owner first (see Resolved Questions). No deviations from the written spec. Live-verified all three response paths' `simple_version`/`pidgin_version` behavior via `curl` against the user's own already-running dev server (a second `next dev` instance for the same project directory refused to start, as Next.js is designed to do — tested against the existing server on port 3000 instead, which only reads the API route and doesn't touch the user's open browser tab's client-side state). Toggle-button DOM rendering, focus states, and the "zero new network requests on toggle" claim were not independently reproduced — no browser available this session.
- 2026-08-26: Wrote and implemented Spec 10 (disclaimer, privacy notice, error/empty states) in the same session. No deviations from the written spec, but one real bug found and fixed while drafting it — raw user message text being logged server-side in three places, violating hard invariant 4 — see Architecture Decisions. Live-verified the new empty-state, disclaimer, and privacy copy via the rendered homepage HTML against the user's live dev server, and confirmed the 400/grounded-answer API paths still work after the logging edits. Chose not to deliberately break the user's live server's API key to trigger the fixed log lines directly, unlike Spec 06's equivalent verification — flagged as a deliberate choice, not an oversight.
- 2026-08-26: Wrote and implemented Spec 11 (test-set script) in the same session, including authoring the 20-query test set from scratch (no prior spec had ever written it, despite being referenced since `SCOPE.md`). Ran it for real against the deployed build: 90.0% overall, 100.0% escalated — both goals met, but investigated the two failures rather than accepting the number at face value, using a standalone diagnostic script (written to the repo root, run, then deleted — never committed) that dumped every chunk for both implicated sources directly from Supabase. Found a genuine content-curation gap in two `kb_topics.json` source URLs (see Open Questions) — not a retrieval bug, not a code defect, and not something fixed in this session since it falls outside Spec 11's scope and touches a protected file.
- 2026-08-26: Wrote and implemented Spec 12 (demo polish pass) in the same session — the final `00-build-plan.md` unit. Asked the project owner whether to add a header before drafting decisions (a scope question, not an implementation detail); resolved yes. No deviations from the written spec otherwise. A real live query incidentally returned 5 citations from the same source during verification, which happened to be exactly the case the citation-dedup decision was written for — traced by hand against that real data rather than a synthetic example. All 12 build-plan units are now complete; remaining work is content/verification decisions tracked in Open Questions, not more build-plan specs.
- 2026-08-26: **App renamed from "Grounded Navigator" to "Beca"**, at the project owner's request — outside `00-build-plan.md`'s 12 units, a branding change rather than a spec. Updated everywhere the name functions as the app's current identity: `components/chat/Header.tsx` (visible header text), `app/layout.tsx` (browser tab title), `lib/ai/prompts.ts` (both system prompts' self-identification — the AI now introduces itself as "Beca," not the old name, in both the answer-generation and urgency-classifier prompts), and the living reference docs' titles/opening lines: `CLAUDE.md`, `SCOPE.md`, `context/project-overview.md`, `context/specs/00-build-plan.md`, `context/architecture.md` (its ASCII folder-tree root was `grounded-navigator/` — also a genuine correction, since the real folder/repo/`package.json` name has been `beca` since Spec 01), and this file's own Session Notes header line. **Deliberately left unchanged, not overlooked:** `supabase/migrations/0001_init.sql`'s one-line SQL comment mentioning the old name — `ai-workflow-rules.md` protects applied migrations from edits, and it's cosmetic only (a comment, no functional effect). Also left alone: the numbered spec files' (00–12) own historical prose — e.g. `12-demo-polish-pass.md`'s Status section literally says "the header renders 'Grounded Navigator'" because that was factually true at the moment it was verified; rewriting historical verification records to match a later rename would misrepresent what was actually checked at the time. `SCOPE.md`'s "**Concept:** Grounded Health Navigator" line was also left alone — that's the hackathon track's own case-study name (Track 4, HealthAccess Initiative), not this project's chosen brand, and not ours to rename.
- 2026-08-26: Implemented the proposed font system (Public Sans / Big Shoulders / IBM Plex Mono, replacing Geist Sans/Mono) after the project owner reviewed a side-by-side Artifact comparison and chose to proceed with all three, reopening Spec 12's original "no second font" call. Found and fixed a real pre-existing bug while wiring it — Geist Sans was never actually applied via the `font-sans` utility, due to a `--font-sans`/`--font-geist-sans` variable-name mismatch dating to Spec 01/12 — see Architecture Decisions. Live-verified via a real dev server: rendered HTML and the compiled CSS chunk both confirmed all three fonts resolve correctly, not just code-reviewed.
- 2026-08-26: Documented and drafted the approved post-Spec-12 "multi-turn triage clarification" enhancement, per `CLAUDE.md`'s rule against undocumented architectural decisions: updated `app-flow.md` (new Clarification state + Journey 4), `project-overview.md` (new "Approved Post-MVP Enhancements" section), `00-build-plan.md` (units 17–18, total 12 → 14), and this file's Architecture Decisions, then drafted `context/specs/17-triage-clarification-classifier.md` and stopped for review — same pattern as Specs 03/04 — without touching any code. In the same session, on explicit instruction ("implement spec 17"), implemented it: `lib/ai/schema.ts`, `lib/ai/prompts.ts`, `lib/ai/classify.ts`, `app/api/chat/route.ts` all modified per the drafted decisions, both ⚠️-flagged items approved as drafted. Found and fixed one real bug during live curl verification (a missing `needs_clarification: false` field in a hand-constructed `.parse()` call, invisible to `tsc`) and explicitly documented one known temporary UX gap (the live-wired frontend doesn't yet recognize the new clarification response shape, closed by Spec 18) rather than silently patching either past this spec's UI-change-free scope. See Spec 17's own Completed entry and Architecture Decisions above for full detail.
- 2026-08-26: Drafted `context/specs/18-triage-clarification-ui.md` on request and stopped for review — one point flagged ⚠️ (`ClarificationCard`'s visual design, no pre-existing `ui-context.md` entry to build against, same situation Spec 12 hit with the header). On explicit instruction ("implement"), implemented it in the same session: `components/chat/ClarificationCard.tsx` (new) and `components/chat/ChatThread.tsx` (modified) per Decisions 1–10, plus `architecture.md`/`ui-context.md` doc updates. `npx tsc --noEmit` and `npm run lint` both clean; a real `npm run dev` smoke test confirmed `GET /` compiles and renders with no error. No browser was available this session, so the live interactive round-trip (clarification card actually rendering, the reply carrying `priorClarification`, the retry path surviving a real failure) is code-reviewed only, not independently reproduced — flagged explicitly in both the spec and this file rather than assumed passing, the same standard Specs 07/09 already set for their own browser-only claims. All 14 `00-build-plan.md` units (12 original + 2 approved post-Spec-12) are now complete.
- 2026-08-25: Reconciled `.env.local` with `architecture.md`/`.env.example`'s documented Supabase variable names. It had drifted to `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (Supabase's default client-side quickstart snippet — wrong shape for this app's server-only design). Renamed to plain `SUPABASE_URL`, dropped the unused publishable key entirely, and fixed a real bug in the URL value itself: it had `/rest/v1/` baked into it, which would have double-appended once `lib/supabase/client.ts` (Spec 05) called `createClient()` — the client library adds that path itself. Also added `SUPABASE_DB_URL` to `.env.example` (it was already required by Spec 02 but never documented there) with a comment steering toward the Session-mode pooler string, not the unreachable direct `db.*.supabase.co` host. `.env.example`'s stray leaked-password line (see Open Questions) was removed in the same pass.
- 2026-08-26: App renamed from "Grounded Navigator" to "Beca" at the project owner's request — see Architecture Decisions above for the full file list and what was deliberately left unchanged (the applied migration's comment, historical spec-file verification prose, and the hackathon track's own unrelated "Grounded Health Navigator" case-study name).
- 2026-08-26: Fixed both real content-curation gaps Spec 11 surfaced, at the project owner's explicit direction with exact replacement values, new topic #8 body content, and the three follow-up steps (scoped re-ingestion, re-run the test-set, update this file) all specified up front. `data/kb_topics.json` topics #2 and #8 updated; `scripts/ingest-kb.ts` gained `--topics=`/`--replace-urls=` and topic #8's `INTERNALLY_AUTHORED_CONTENT` entry; ran the scoped re-ingestion for real against the production Supabase database (verified all 12 topics present afterward, the other 10 untouched, chunk-count arithmetic exact); re-ran Spec 11's test-set script for real against the deployed build — 100.0% overall, 100.0% escalated, both previously-failing queries spot-checked live and confirmed correct. The project owner's step 3 also said "then move to drafting Spec 12" — flagged rather than silently actioned or silently ignored: Spec 12 was already drafted, implemented, verified, and pushed to `main` earlier in this same session (see its own Completed entry and commit `aeb2bad`), so there was nothing left to draft.
- 2026-08-27: Ran a full architecture audit against a reported bug (an exercise/physical-activity question incorrectly refused) before changing anything, at the project owner's explicit "audit first" instruction. Presented the audit and a phased fix plan; project owner authorized Phase 0 only (KB expansion, ~8–10 topics) via explicit follow-up questions, not Phase 1/2 (hybrid retrieval, query expansion, threshold tuning, intent classification — deferred, see Next Up). Implemented Phase 0: `data/kb_topics.json` expanded 12 → 21 topics with a richer, fully-populated schema across all 21 entries; every new WHO URL individually confirmed live via `WebFetch` before being committed (none guessed at from a URL pattern); `scripts/ingest-kb.ts`'s `KbTopicSchema` extended to validate the new fields. Ran the scoped ingestion for real against the production Supabase database and verified the exact reported failing query now returns a correctly-grounded, correctly-cited answer; re-ran Spec 11's full test-set afterward with no regression (100.0%/100.0%, unchanged). See the new Completed entry and Architecture Decisions above for full detail.
- 2026-08-27: On request ("write the spec file"), drafted `context/specs/19-hybrid-retrieval-fallback-diagnostics.md` for the audit's deferred Phase 1 work, and stopped for review — same drafted-then-wait-for-"implement" pattern Specs 17/18 used, no code/migration/schema touched. Registered as build-plan unit 19 in `00-build-plan.md` (depends on 05; total-units line updated to flag it as proposed, not yet counted as approved). Two genuine judgment calls flagged ⚠️ inside the spec itself rather than silently decided (the keyword-rescue slot numbers; keyword-containment expansion vs. a constrained topic-classification LLM call as a fallback alternative). Also fixed a stale "12 topics" reference in `project-overview.md`'s Must-Have feature list (Phase 0 changed this to 21) while in the area — a factual correction, not a scope change.
- 2026-08-27: On request ("resolve #1 and #2 now"), asked the project owner to choose between the spec's drafted defaults and the alternatives for both flagged points, rather than deciding unilaterally — both were confirmed as drafted (`match_count` 8/3, keyword-containment expansion). Updated `context/specs/19-hybrid-retrieval-fallback-diagnostics.md`'s ⚠️ markers to ✅ with the confirmation recorded, and this file's entry above. Implementation still not started — decisions being resolved is a separate step from an explicit instruction to build, per this tracker's own established two-step pattern.
- 2026-08-27/28: On request ("go ahead"), implemented Spec 19 in full. Discovered mid-implementation that no Postgres client (`psql` or a Node library) existed anywhere in this environment/project to apply a raw DDL migration — added `pg`/`@types/pg` as devDependencies (justified in the spec file per code-standards.md hard invariant 5) and wrote a throwaway runner script (deleted after use, not committed) to apply `0002_hybrid_search.sql` through the already-configured `SUPABASE_DB_URL` pooler connection. Verified the migration live via direct catalog queries and a real RPC smoke test before touching any application code. Re-ran ingestion for all 21 topics to backfill `kb_sources.keywords` (115 chunks, unchanged — confirmed no content drift from the re-run itself). Rewrote `lib/kb/search.ts` and updated `app/api/chat/route.ts` per the spec. Verified live: the exact originally-reported query and a deliberately awkward paraphrase of it both return correctly-grounded, correctly-cited answers; case D's new log line confirmed firing in the real server log with only `message.length`, never message text; Spec 11's full test-set re-run with zero regression. Updated `database-schema.md`, `architecture.md`, the spec's own Status/verify checklist, and this file — all per the established sync-table convention.
